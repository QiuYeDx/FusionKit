# 保守来源选择接线结果

## 2026-09-07：实验链已接通，未获得新增实样修复

按 [本批合同](group-selection-contract.md) 新增 `overlap-group-selection.ts`，将来源预检和独立证据判断接入 `production-executor.ts` 的停顿实验链。只有 `rootPlan.schemaVersion === 2`、Windows/CUDA/large-v3、转写/VAD 开启的现有增强路径进入此分支；默认固定策略以及短音频、VAD-off、超长音频回退不变。

预检对左右两侧分别调用已有拼接草稿校验，只有能够完整包含另一侧的来源才进入选项。没有选项时零额外观察，不读附件或使用用户短语白名单。复核后仅一侧通过就选该侧，两侧均通过时规范化文字序列相同，稳定保留左侧分句；不按“更长”“更短”“置信度更高”来裁掉独有内容。

新分支位于既有五种重叠修复之后，排除已被修改的原始 cue 索引。每任务最多一组、两次固定 20 秒原音观察，分别请求 `freshInferenceState: true`；复用已有共享请求预算，保守要求关联根尚未消耗可选请求，不额外放宽根预算。无增益、no-VAD DTW、协议校验、保留数据预算与清理行为沿用现有执行器；加载或识别不可用时保留原结果，取消/清理失败阻止导出。

## 验证范围

- 6 个文件、324 项测试全部通过：生产执行器 149，拼接/选择 30，源组提名 18，停顿计划 5，后处理 85，服务协议 37。
- 新增选择器测试验证左侧独有清嗓、右侧独有清嗓、等价来源稳定选择、缺少独立复核时不选。
- 新增 11 个模拟执行器场景覆盖成功、两组候选最多处理一组、默认路径禁用、词形分歧不请求、共享预算耗用后跳过、缺失/漂移点、加载失败、识别失败、取消及清理失败。成功场景从模拟原始观察到实际执行器的 exporter 调用不再手填 `keep`，得到整句 25800～29000ms 的一条结果。失败场景保留原三条，取消与清理失败不导出。
- 增强固定 20 秒裁窗和两次 fresh 状态断言后，定向 11 项再次通过；未声称重新跑了一遍全部 324 项。
- 全项目 `tsc --noEmit` 通过。未修改 UI/IPC、默认策略或运行配置；未启动真实模型、Vite、Electron，也未做新的完整应用或原生模型正例验证。

## 真实存档复核

复用八素材已有最终导出和原始窗口观察，按最终 cue 投影调用实际新提名/来源预检。七份有导出的素材中只有旧 full 的两个已知接缝被提名，左右均无完整包含选项；noise 仍为无导出。重新读取上批四个原生响应，第一份仍被真实协议拒绝，其余三份有效；两组调用选择器均为 `no_complete_source`，不因人工认可改成自动通过。

这是**最终导出来源扫描**，不是完整执行器回放；它发生在此前最终显示处理之后，不能等同于新分支执行位置的全部候选/预算判断。证据保存在私有 `pause-production/group-selection-replay.json`，脚本为 `test-results/group-selection-replay.ts`。没有覆盖旧存档，未新增模型请求；已获认可的整轨离线参考仍保持独立。

因此本轮可以确认执行接线和失败边界，不能声称这些真实素材的自动转写效果又有提升。两个具体拼接结果已获人工认可，通用的异体来源选择仍未解决。它们继续作为固定对照保留，不再追加针对它们的字形规则、阈值搜索或模型重试。

## 后续边界

当前完成的是完整包含情况下的保守自动选择及实验执行链。默认启用 P3 仍未通过；新入口的真实模型正例和新版本完整应用验证尚缺，不启用默认。不能把模拟执行器成功或人工修过的参考文件当成正式应用成功输出。

本阶段这条复核路线先收敛：保留已完成的停顿计划、拼接和保守选择代码，以及两轨停顿实验收益；剩余差异拼写重复作为明确限制。后续若要继续扩大自动修复范围，需要独立、可泛化的新证据方法；不再安排相同两段的重复听校。此记录不授权删除旧规则、切换默认策略或迁移到新的推理后端。未提交、未推送。
