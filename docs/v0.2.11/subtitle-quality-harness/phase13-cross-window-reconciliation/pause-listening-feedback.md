# 停顿优先切块：人工听校反馈与下一批范围

## 2026-09-06：六段听校通过

用户提交 `asmr-pause-listening-annotations.json`。共享 `validateStageAnnotations` 合同实际验证通过：包身份、六段顺序、音频/字幕指纹、时长、时间域和全部评分字段匹配。

- reviewId：`2a92343d1de92d4a98b9e0d9f3dd4621003bdcf1cc74bd670fba7f0d910abddf`
- 附件 SHA256：`6e5374e960052a965b6c65449068cd5e6e191b383c267b7c45d79418de54e45b`
- 导出时间：`2026-09-06T15:36:29.056Z`
- 原字节归档：私有 `test-results/subtitle-quality-review/new-track-seams/pause-review/user-annotations.json`，读取后逐字节复核一致；汇总为同目录 `user-feedback-summary.json`。

| 片段 | 原轨范围 | 分句 | 可读性 | 时间轴 | 内容 |
| --- | --- | --- | --- | --- | --- |
| t-one-a | Track1 00:40～01:08 | good | good | good | good |
| t-one-b | Track1 01:33～02:16 | good | good | good | good |
| t-two-a | Track2 01:35～01:58 | good | good | good | good |
| t-two-b | Track2 02:18～02:44 | good | good | good | good |
| t-two-c | Track2 03:13～04:08 | good | good | good | good |
| t-one-risk | Track1 07:15～08:20 | good | good | good | good |

六段共 240 秒，24 项均 good，全部备注为空、无问题标记。此前纳入风险听校的剩余重叠附近片段也得到认可，不能继续把这段记为用户反馈失败。播放历史仅为 interaction_only，不从拖动/暂停记录推导逐词边界或完整收听证明。

接受本包六段的分句、可读性、时间轴和内容效果。结合固定实验中 11 个重复文字锚点由 2 次降为 1 次，支持继续推进从切块源头减少重复的方向；不是全轨、全部素材或逐词准确率证明。本轮不重调阈值，不要求重听相同六段。

## 下一批：先回归，再接入生产

1. 固定当前实验策略版本与参数，在此前六素材上复用已有原音/PCM 和已验收产物建立回归清单。先离线规划，检查样本完整覆盖、每块处理上限、停顿切点缓冲和无法找到停顿时的重叠回退。低声尾音、独立清嗓/真实重说、ASMR 音效和非语音对照必须纳入，不能把能量低直接标为确认静音。
2. 对实际切块变化的素材做一次有界新解码，保存全部响应，沿用同模型、VAD、增益回退规则和每轨独立进程。和已验收结果对照漏词、重复、分句及时间变化；不为了获得好结果反复改阈值。只有出现新的待判断变化才补有针对性的听校。此项尚未执行。
3. 正式接入前明确规划版本/身份、原始样本坐标、核心区唯一归属与输入覆盖校验；保留固定计划兼容校验，不能通过删除 `validateRootPlan` 或伪造固定窗口身份接入可变计划。重试子窗继承父范围、取消/预算、进度和可选增强对固定窗口的假设一并检查。
4. 以真实生产 executor 路径验证可变计划，并回归已验收的分句、去重与时间轴增强。确认通过后再启用默认策略并交付阶段产物；无停顿的连续语音仍保留重叠和接缝处理。

停止条件：若出现明确漏语音/尾音回归，保存失败样本并暂停默认启用；先判断切点证据是否不足，不进入无界参数搜索。当前证据已经支持下一批开发，无需用户再次确认相同实验方向。

## 当前产品状态

本轮仅接收反馈、归档和更新开发记录，没有新增推理或修改默认转写路径。`pnpm dev` 目前仍运行原有固定窗口策略；本次通过的停顿优先策略仍在 benchmark 实验中。原附件和 NAS 文件未修改，未提交/推送。
