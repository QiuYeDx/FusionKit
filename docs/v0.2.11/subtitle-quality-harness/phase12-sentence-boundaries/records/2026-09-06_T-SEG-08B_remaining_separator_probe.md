# T-SEG-08B：剩余三处分隔的固定观察诊断

## 实验约束

依设计6.36预先固定三窗口，在08A真实应用退出后串行运行。每窗口fresh进程，现有生产DTW large.v3/non-VAD字段，一次请求无搜索/重试。只裁工作区PCM副本；不修改NAS、主转写正文、默认预算或08A实际导出。原始响应、全部异文及额外文字保留，不能只展示相符部分。

## 结果

| 目标 | 固定窗口 | 观察与结论 |
| --- | --- | --- |
| independent第19条位置3 | 110～140秒 | 完整目标文字一致，但仍无分隔；相邻词元点间隔860ms不是实测静音长度。旧multi119有逗号，但完整原生段横跨本条及下一条；multi120已从短句后一部分开始，不能算第二份完整头部证据。 |
| independent第23条位置8 | 145～175秒 | 首段仍无分隔，点间隔940ms与139/140秒旧观察相符。稳定点间隔不能独立证明短语/停顿；窗口末还出现原主正文没有的结束语，完整保留审计但不采纳。 |
| control第3条位置6 | 0～30秒 | 原生输出分成两个段，但前段和后段均有异文/额外词，不能完整对应已接受正文。既有完整组规则拒绝这类结果是必要的，不能只把空白问题归因于时长门槛。 |

C显示了“有原生段界，却无法可信对应原文”的问题类别；本实验是fresh离线观察，不是旧应用的内部追踪，不能声称已证明其某次失败的确切分支。背景音存在不证明连续说话，也不应被自动删除或当语音补写。

## 验证与下一步

恰好三次请求，返回段数6/6/3；输入哈希前后相同，输入来源与裁片偏移已记录。三进程89056/71904/64668退出；与08A的Electron共四个记录PID在最终只读检查中均不存在。无新增生产行为。

私有证据：`test-results/subtitle-quality-review/phase12/display-separator-probe/{inputs.json,A-short.json,A-cost.json,A-control.json,A-report.json,audit.json}`；原始全窗文本和绝对DTW点均可重放，不以只筛目标的摘要替代原响应。

下一步08C先构造“最终邻接条目组成完整上下文”的精确对应合同，用于找回跨条目的原生结构证据；对缺少显式分隔的点间隔和C异文分别保持未支持，不能靠扩大相似匹配范围修复。确需新观察时必须在源码接线前列出入口、固定窗口、每文件请求上限及对六素材原流程的预算影响，不能把本次三个诊断窗口直接硬编码进生产。无需再次索取用户已经明确给出的五处分隔目标。
