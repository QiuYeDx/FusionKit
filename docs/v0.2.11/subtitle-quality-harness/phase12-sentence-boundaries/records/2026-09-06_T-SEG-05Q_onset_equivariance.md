# T-SEG-05Q 开头起点的等内容平移对照

2026-09-06，R-SEG-02/03/05/07，M 单模块串行。05M～05P 的 9224a64 已推送 origin/v0.3.1，本轮按设计 6.27 固定四次诊断请求，默认输出不变。

## 输入与执行

三份开头素材均保留 opening-0-18.wav 的完整 18 秒 PCM，仅分别补 0/1000/2000 ms 数值零。16000 Hz、单声道、16 bit；逐帧核对剥离前补后字节完全相同。第四份为已有 B-noise 的完整 18 秒前补 1000 ms。原件与 NAS 未修改。服务器、模型与正式字段均记录 SHA：server c72f99b75ae4111d6e7ddb62e04d3c0c94fea2ba6536412e517f256b4388dd37、model 64d182b440b98d5203c4f9bd541544d84c605196c4f7b845dfa11fb23594d1e2。

四次新进程请求全部完成，未追加观察或改参数。加载/推理分别为 2661/1867、2557/1459、2552/1159、2547/519 ms，非性能基准。未补静音的新请求与 05B 相同输入的完整文字、段边界、普通词边界及 DTW 点均复现。这排除了本次比较中基线重现差异，但不是对所有运行的确定性保证。

## 观察结果

以下都已扣除前补静音，单位 ms；DTW 为 token 内点，不是人工测得的词首。

| 前补量 | 原生段数 | 私 DTW | だ DTW | 私普通 start | 私所在原生段 start | そうだ DTW |
| --- | --- | --- | --- | --- | --- | --- |
| 0 | 7 | 7560 | 8040 | 6370 | 6000 | 9780 |
| 1000 | 4 | 7480 | 8040 | 6000 | 4000 | 9760 |
| 2000 | 1 | 7440 | 8060 | 7390 | -2000 | 9740 |

目标“私だ”首点跨度 120 ms、尾点跨度 20 ms，“そうだ”首点跨度 40 ms。普通“私”start 跨度 1390 ms、普通“そうだ”start 跨度 560 ms。补 2000 ms 后整段覆盖输入起点，映射到原音为 -2000 ms；报告保留该值，没有钳到零后冒充精确边界。原生分段和普通词起点不具备本例等内容平移稳定性。

整窗文字不一致：问候长音/标点、末尾“知ってる／知っている／知ってた”等均变化。三份均有“ん、ん”，点约 5420/5840、5420/5840、5440/5860 ms，不能仅凭多份一致就升级为已确认台词。

旧 4～18 秒观察仍完整保留：没有上述两处“ん”，原文含额外“え?”；“私”点 6360 ms、“だ”点 8060 ms。与新三份相比，尾点保持，但私→だ的点间距为 1700 ms，而等内容三份为 480/560/620 ms。因此问题更具体地集中在首 token 的分配，而非整句统一平移。前部字词覆盖改变与首词向前延伸同时出现，支持“上下文不全时首词吸收前部时间”的待验证解释；尚不能据此认定旧点必错、语气词必真，或制定按某个持续时间直接丢弃观察的规则。

负对照由旧“もやし”四次变为“おやすみなさい”。保留两份全部输出，均不接受为语音证据。补静音不是可上线的识别增强方法，也不能因本例目标点稳定就替换全文或默认裁窗。

## 审计、验证与采用决定

新增 onset-shift-audit.mjs：严格绑定内容与协议 SHA、原音范围、唯一补入量及基线，最多八份观察。完整 token 覆盖、唯一目标位置、普通/DTW/原生时间并列、无效/倒序/越窗点、目标落入补入区、负映射及全文差异全部显式记录。报告恒 automaticAcceptance:false，不输出字幕或推荐切点。私有样本目标通过参数传入，通用工具没有样本文字分支。

25 项新测试加既有局部时间/完整组 33 项，共 58 项通过；包含后一次失败、跨 token、缺字、重复、各类时间错误、来源/字段不符、同目标但全文变化、缺基线、重复/超限计划和整数溢出。本批只改离线工具和文档，没有主进程/UI 变化，未重跑 Electron 或沿用 05P 的 375 项冒充本轮测试。设计写入时原 Spec 检查为 0 error/0 warning；收尾时外部技能检查器已于 18:20:57 更新为仅支持 spec.json 的 v2，本项目沿用的 Markdown 规格缺少该文件，故新检查器不通过。未擅自迁移历史规格；另核对本增量任务依赖、需求编号、设计章节、记录链接和实现文件，私有 document-check 通过。git diff --check 通过。这不是声称新版完整检查通过。

采用决定：05Q 诊断完成，不接入生产，不生成一个绕过旧分歧的听校字幕。等内容实验把普通词边界的不稳定与首 token 延伸问题区分开，但它们仍来自同一个模型，不能作为独立的词存在或词首证明。下一项应核验目标前部完整语音覆盖及首词被拉长的原因：在原音中独立确认约 5～8 秒的语气/停顿与“私”的实际起始，再检查所有观察对同一次发言的分配。不得继续追加有利裁窗、简单多数表决、平均点或只按持续时长淘汰 6360 ms；必须先补充能解释分歧的证据。已验收后续句界和全部接缝修复保持。

四个 PID 82852/84656/80284/69040 均已退出，专用脚本确认所属原生进程为零。第一次内联命令扫描误匹配检查命令自身，改用脚本文件和原生进程名过滤后验证通过，没有终止用户进程。未创建模型副本；输入、完整响应、audit、protocol/资源指纹和 process-check 保留于 test-results/subtitle-quality-review/phase12/onset-equivariance。NAS 三份源文件 SHA/大小/mtime 保持。

本轮 05Q 新增工具和文档尚未提交；用户先前要求的阶段代码已完成提交与推送。
