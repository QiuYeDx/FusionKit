# T-SEG-06D：字幕预览弹窗重做

## 后续密度调整

按用户反馈，字幕行上下 padding 从桌面各 20px 改为各 6px，时间行高从 20px 改为 16px，正文行高从 28px 改为 24px，保留正文字号。桌面短条目实测约 81→45px，同一窗口可见约 7→12 条；窄窗压缩时间与正文间距，长句继续自然换行。样式构建和隔离 Electron 桌面/深色/390px 检查通过，截图为 `reading-compact-*.png`。重放已有响应，无新增转写；PID 50932 已退出，所属进程为 0，测试 profile 已清理。

用户要求彻底替换任务行预览按钮打开的简陋弹窗。原预览实现已移除，独立组件负责新的字幕阅读界面；入口和产物引用读取合同保持。

## 交互与布局

- 默认按完整字幕条目阅读：序号、时间、正文分栏；窄窗口上下排布。保留多行、重复台词和字面标记，LRC 不推测结束时间。
- 搜索正文或时间并高亮命中，按 60 个完整条目分页。清除搜索后焦点仍留在输入框；翻页回到顶部。
- 独立原始文件页保留 SRT/LRC 原文，原文分页避免截断 Unicode 代理对；解析失败自动回退原文。
- 固定顶部标题、切换和搜索，以及底部条数、翻页和复制；正文区域独立滚动。超长文件名省略显示并保留完整 title。
- 阅读页复制全部正文，搜索不会缩小复制范围；原文页复制完整源文件。支持成功反馈、失败提示、读取重试和旧请求隔离。
- 关闭弹窗后焦点返回原预览按钮。ScrollableDialog 只新增可选 onCloseAutoFocus 透传，既有调用行为保持。
- 四语言文案同步，浅色、深色、390px 窄窗与 780×540 矮窗均检查。

## 验证及证据

解析/分页/旧导出兼容 7 项测试通过。使用真实 Electron 新构建与隔离 profile，对工作区 opening 音频副本完成一次真实转写（13 条），由真实任务入口打开实际产物。其后 UI 测试重放保存的真实响应，并显式使用 125 条、延迟、读取失败和 LRC 等测试数据；没有反复运行 ASR。

交互验证覆盖搜索、全文/原文复制、60/60/5 分页、滚动复位、字面 HTML、错误重试、关闭后旧请求丢弃、剪贴板失败、长标题、短窗口与焦点返回。Windows 剪贴板会规范化换行，断言在比对时处理 CRLF；异步复制等待成功状态。长标题测试数据需同步 completion 与 artifactResults 才符合现有严格 IPC 合同。

私有截图与报告位于 `test-results/subtitle-quality-review/phase12/preview-redesign/`，不入版本控制。截图等待加载及开场过渡结束。模型、字幕内容及转写策略未修改，也未写入 NAS。

TypeScript、Vite 构建、preload 检查及四语言 key 检查通过；i18n source usage 仍有 Q-SEG-CHECK-01 的 Rename 页面动态 key/过时清单两项既有问题。最终 UI 重放 PID 51028 已退出，所属进程为 0，已删除约 5.38 GB 测试模型副本及隔离 profile，保留截图和报告。沙箱中的一次类型检查曾误报已存在的依赖文件缺失，在正常读取权限下复核通过。本轮 UI 工作尚未提交。
