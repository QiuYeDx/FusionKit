# T-SEG-07B：综合听校附件核对与剩余可读性问题

## 附件与身份

用户提交 `asmr-stage-listening-annotations.json`，schema 为 `fusionkit-stage-listening-v1`，reviewId 为 `9eebcf0c1b7f5e56605d24e5088e04f4c62c9bbaaa8a4e0e4cc401b80e4f17c9`，导出于 `2026-09-06T13:09:35.496Z`。附件 SHA256 为 `1ffe0abe9baf3ee8c3317af44ddeb7eb66619b0c5ebb599a26bba624f68e3129`。

共享合同验证五段顺序、音频/字幕指纹、时长、时间域、评分与备注。另重新计算 HTML 内嵌音频、当前 SRT/LRC 和整包元数据指纹，与 manifest 一致。用户附件原字节已归档为私有 `stage-listening/user-listening-annotations.json`；`user-listening-validation.json` 保留完整评分、备注与问题映射。没有覆盖合成 QA 文件，未修改 NAS、原始字幕或附件。

## 验收结论（限定这五段和当前版本）

| 维度 | 可以接受 | 仍有问题 | 不确定/未填 |
| --- | --- | --- | --- |
| 词句截断 | 5 | 0 | 0 |
| 短句可读性 | 3 | 2 | 0 |
| 时间同步 | 5 | 0 | 0 |
| 漏词/额外词/复读 | 5 | 0 | 0 |

问题全轨、低声 B、音效对照四项均为 good。独立序章和局部低声 C 的 readability 为 issues，其他项 good。用户共标记四条字幕、五处至少应有空格的短语边界；独立序章另有整体“偶尔应有空格而紧挨”的备注。因此接收分句、时间、内容三维的本组实样认可，但 R-SEG-02 和整阶段不能全部关闭。20 项中 18 项 good 不是模型准确率，也不能推断其他录音或平台通过。

## 四条问题的准确归属

| 素材/条目 | 当前字幕区间（ms） | 用户定位（约 ms） | 要求插入的 UTF-16 位置 | 目标 |
| --- | --- | --- | --- | --- |
| independent 第 19 条 | 117280～121980 | 121375 | 3 | 安慰短句与后句之间 1 处分隔 |
| independent 第 21 条 | 133200～143440 | 141424 | 17 | 条件短语与后句之间 1 处分隔 |
| independent 第 23 条 | 153360～164820 | 155357 | 8、13 | 代价说明的三段短语之间 2 处分隔 |
| control 第 3 条 | 8390～19410 | 18680 | 6 | 音效隔开的两个话语部分之间 1 处分隔 |

逐条从用户备注中的短语提取、拼接，均与当前字幕正文唯一且逐字相等；插空格目标去空格后与源文相同。精确短语、原话、展示目标在私有验证 JSON 内保留。目标只是用户期望的显示示例，没有生成“已修复字幕”或把人工短语写入默认规则。

遵守听校避坑规则：用户定位是近似问题位置，不是内部词界时间；history 是 interaction_only，不推断其完整听到范围。C 的备注明确中间仍有 ASMR 音效、没有有效话语，因此不能把“非静音”直接判断为连续话语。此次要求是至少显示分隔，不要求重新拆字幕条目或移动已认可时间。

## 只读源码与历史观察审计

1. `cue-separator-restorer.ts` 的 `needsLocalSubtitleSeparators` 要求时长至少 6000 ms；117280～121980 的 4700 ms 源条目无法进入该文本补分隔入口。文本分隔与新增时间切点共用较长片段资格，遗漏短句内可读边界。
2. `production-executor.ts` 先处理与原父段文本和时间完全相等的分隔候选，再执行 multi/contained/variant 消费者；后者替换的最终完整长句没有末次文本分隔处理。133200 和 153360 两条来自已验收的后置去重结果，早先父段分隔入口无法直接覆盖最终条目。
3. multi119 的旧观察在用户指定位置有逗号，multi120 有原生段界；variant139 在后一处有原生段界，variant140 在相应位置有空格。说明已有值得复用的结构线索，而不是必须一开始增加推理。它们是保存的历史固定观察，不能冒充最后一次生产的内部追踪；还包含专名/字形和额外前后文差异，不能直接替换主正文。
4. C 的 11020 ms 源条目满足时长/长度资格。具体未补全原因尚需追踪原文映射、候选证据与可选请求早停，不能归因于 6 秒门槛，也不能仅凭音效能量推断。

审计结果在私有 `readability-source-audit.json`，零新增推理、默认代码未改。这是问题定位与反馈收束，不宣称五处分隔已经上线。

## 后续 T-SEG-08 范围

- 首先以当前最终 cue 与已有完整观察建立仅插入空格/标点的源位置计划，保留原文、原标点、条目 ID、数量与起止；后置去重的最终正文也能携带合格的分隔证据。
- 分开“显示分隔资格”和“新增时间边界资格”；短于 6 秒也可能需要空格，但不能只下调阈值后给所有短句新增识别请求。优先复用已有观察；无依据时保留，任何新请求必须另列有界预算。
- 对少量字形差异只能复用既有受限对应做位置映射，不授权改写名字或从异文切出随意子串。对 ASMR 音效间隔要求文本/原生结构佐证，不用 VAD 或能量独立插空格。
- 回归固定这四条五处作为人工反馈目标，并增加短正常句、词中断点、真实重说、含音效长停顿、已有标点/空格、未知对应等反例。已有五素材正文和时间作为保留基线；不把历史明确失败目标改成“无需分隔”以让测试通过。
- 完成后先核对其他输出保持，再交付必要的局部可读性对照；不重做本轮已认可的全部时间定位或再次索取相同短语描述。

本轮验证：共享标注合同实际导入成功、音频与字幕重新哈希、四条唯一匹配与五处字符位置核对、原附件字节归档核对。未启动应用、浏览器或推理进程；没有提交/推送新改动。
