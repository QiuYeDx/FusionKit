# T-SEG-05G：完整候选组接缝默认消费

2026-09-06，关联 R-SEG-02/03/04/07，方案见 6.17。用户提供 asmr-full-seam-listening-annotations.json，内容 resolved、时间 aligned，77370～81000 ms 的完整句与实验身份逐项一致，已原样归档并保存验证报告。automaticAcceptance:false 为固定导出字段，不是用户否定。只对这一处沿用听校，不扩大为全轨准确性结论。

## 默认接线

新增 cue-complete-overlap-evidence.ts，提取 05F 已测试规则为共享类型化核心；离线审计用已有 TypeScript 转换同一源码，仍返回未自动验收的提案。生产不依赖 benchmark。review 记录右原窗口起点，避免把 ownership 裁定的 seam 时间当作台词起点。

cue-overlap-resolver.ts 优先执行旧双侧单段规则，其成功结果保持；仅不接受时检查完整连续候选组。合格后使用已有右原始观察的文字、起点和终点，保留原有接缝替换 ID 规则。没有硬编码本例文字/77370 ms，没有调整阈值、请求资格、原生顺序、freshInferenceState、最多两处接缝或每根窗共享预算。执行器实现未修改；不通过的证据继续保持原字幕。

## 验证

201 项相关测试通过：接缝消费者 22、执行器 90、共享离线规则 15、格式 25、导出 49。新增生产案例在不同时间原点验证完整多段成功、变词/额外发言/重复/起点不一致回退，执行器仍只用两次主识别和一次接缝请求；旧取消、启动失败、清理失败、请求顺序和共享预算案例保持。保存的真实接缝响应回放精确复现已听校候选，真实 B-noise 候选拒绝。

初次 TypeScript 检查指出共享核心两个 token 回调缺类型标注，已补齐并重新检查通过。随后 Vite 前端/主进程/preload 三段构建及 preload 外部依赖检查通过，仅保留既有 chunk/dynamic import 警告。产品 UI 和语言文案未改，不借此关闭原全库 i18n 未决项。

新构建通过实际 Electron 默认页面运行六素材：large-v3/CUDA、日语、Silero VAD，SRT+LRC。production-complete-overlap/verification.json 严格核对：完整音轨 67→66 条，唯一变化是把 74610～77500 ms 残缺变体与 77500～81000 ms 完整句替换为 77370～81000 ms 完整句；其他 65 条文字/时间逐条完全保持。完整 SRT 解析结果与 05F 人工通过的实验整轨逐条相同，无新增待听校差异。

开头 12 条、独立全轨 31 条、低声 B 1 条、保存 C 3 条，SRT 与 05E 字节一致；无词音效仍 no_speech_detected，不输出字幕。每份 LRC 与本轮 SRT 文字/起点对应，已验收的开头各时间及 24960～31230 ms 一次完整句保持。三份 NAS 原件大小、修改时间及 SHA256 均不变。

实际应用 PID 56148 已关闭，无页面错误；六素材批次首快照至终态 99.624 秒，包含加载/排队/推理，不作速度收益结论。这是新构建默认应用验证，不是安装包发布验收。

## 收尾

T-SEG-05G 完成，本处听校收益已进入默认路径。进程表确认本轮原生/应用进程退出，验证工作区专用配置绝对路径和无 reparse point 后清理 app-profile；字幕、音频副本和证据报告保留，详情见 cleanup-report.json。本轮未启动前端服务，未提交或推送。

整个 T-SEG-05 仍进行中：本例两处跨窗重复已有实际修复，跨父段完整句规划、开头“私だ”独立定时及全阶段验收仍未全部解决。下一步继续处理这些剩余范围，不重复要求用户听校相同的 77370 ms 结果。
