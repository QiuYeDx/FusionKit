# T-SEG-05T 短句起点默认接线

2026-09-06，按设计6.30，承接05S完整原音证据核验；用户授权连续推进。默认接线、真实PCM读取、预算/回退测试与六素材实际回归完成后才关闭本任务。

## 实现与边界

基线为 v0.3.1 / 9224a645ef1b2db055ed25c038b614e02ddad105 加当前05Q～05T工作树；未提交、未推送。05S已在同一工作树通过证据重放。

生产执行器在既有分隔、接缝、前缀、多父段、包含及异文修复之后，只检查最终首条的独立短尾句。复用第一份已有0～30秒DTW结果；来源与原始PCM检查合格才追加一次fresh的0～18秒原音复核。没有后文、字数/时间不合格、ID冲突或无合格活动时不新增识别。新窗口有独立结构身份，但请求计数明确归入原首根预算，最多由2变3，不影响此前请求顺序。

媒体层通过真实有效proof读取最多30秒PCM16，生产取前18秒；验证文件身份、读取长度、取消及关闭句柄。不得使用增益后的临时音频。普通可选失败保留原输出；身份变化、取消和清理错误阻止导出。这里保留全部源文字，并未把两声清嗓音识别为幻觉或删除语气词，也未决定新增清嗓字幕的表现形式。

新增测试先发现测试响应未采用服务包装、增益fixture误入原有回退分支，随后发现短观察结构的rootWindowKey需与独立windowKey一致。修正测试接线和结构身份，预算仍通过单独参数归入首根；未放宽资格或改变预期。

## 验证

- 新核心33、生产执行器130、媒体层46，共209项通过；随后补充“无后文”独立边界测试1项通过，共210项相关测试。成功、首点分歧、词点缺失、无活动、普通PCM失败、文件变化、启动/识别失败、取消、清理失败和有限请求均覆盖。其余已有120项生产测试保持通过。
- 听校绑定与平移审计57项通过，保留用户澄清与原始附件的区别。
- TypeScript、`vite build --mode=test`、`check-preload-bundle.mjs`通过。构建只有原有混合导入及包体大小提示，无新增界面/i18n改动。
- 实际Electron新构建、隔离userData，large-v3 / CUDA / ja / VAD开启，SRT+LRC同时导出。六素材均达到预定结果：opening 12→13、full 66→67，唯一变化都是将首条分成2500～7400 ms「ああもしもし」与7400～9660 ms「私だ」；其余条目逐字段完全保持。independent 27、quiet 1、control 3条与05P SRT字节相同。noise仍为no_speech_detected且无SRT/LRC。各输出SRT/LRC起点及正文一致，全部源文字保持。
- 实测窗口物化数5/20/1/1/2/13，只有opening/full各增加一次。此为文件观察，非HTTP请求仪表；执行器测试另验证预算。批次快照间147751 ms，旧05P为150910 ms；不同运行波动，不将此称为提速。
- NAS三文件SHA256、大小、mtime及独立原音SHA保持。仅工作区副本输出字幕，没有修改NAS和下载附件。
- 测试Electron PID25440已退出，关联配置路径的进程数0，隔离复制资源5379039014字节已清理。首次Windows PowerShell5默认编码读取JSON失败，PID未取到；随后明确UTF-8的只读检查补核实际PID和目录归属，更新清理报告。已将编码/终止错误/非空PID要求记录为项目避坑案例，未重复删除。

私有证据目录：`test-results/subtitle-quality-review/phase12/short-onset-production/`，含`app-report.json`、`verification.json`、`cleanup-report.json`及`app-inputs-isolated`实际字幕。自动7400 ms与人工主观7380 ms的20 ms差值不等于测得准确度，不冒充新人工签收；无须重复标记同一原音。

最终类型及`git diff --check`通过；`document-check.json`保存六个实现/测试文件的工作树SHA256，并检查05S/05T状态、需求、依赖、设计段和记录引用。沿用旧Markdown权威台账，未运行不兼容的v2规格检查器；该检查只覆盖当前增量，不代表整个phase12验收。

05T完成，默认源码路径已有本次修复，未打包发布或提交。T-SEG-05整体与后续设置说明、格式/翻译交接及综合验收仍按总台账推进，不宣称所有转写问题已经根治。
