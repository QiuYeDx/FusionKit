# T-SEG-05R 前部声音与词首定位听校

2026-09-06，R-SEG-02/03/05/07。按设计 6.28 构造独立原音标注；没有新增识别请求，没有默认字幕改动。用户连续推进授权沿用，人工听感另行记录。

## 交付与证据边界

新增 onset-listening-review.html 和 onset-listening-contract.mjs。私有生成页内嵌原 0～18 秒 WAV 的原始字节，音频 SHA 为 6e3f895204c01b14ab11dc9533213497626755f0193097a6922c8861581f662e，母媒体 SHA 为 9ae30957514dd75faa08464f3aeee6025cb2d9bdb98ae98bd703649451ded527。manifest 的原音偏移、时长、4～9.66 秒播放范围、5～8.6 秒标记范围、20 ms 波形 bin 和完整幅值数组共同绑定 reviewId：8ec4830af8d8b951c03c971aad4f6943a53c641e92641ec4acc11ab70b9295fd。

页面起点与答案初始为空，不画模型候选线。用户可点波形、输入秒数、在暂停处标记、50 ms 微调，从标记处或提前 300 ms 试听。越界输入提示并恢复原有效标记，避免输入框与导出值不同；也可清空标记。音频控件保留完整 18 秒，可自由回听。

前部声音与主观起点置信分别填写，无法判断可留空起点。按同一 reviewId 保存草稿，恢复时核对枚举、范围和有限日志，导出不依赖浏览器存储成功。日志区分请求范围、实际 play/pause、手动/按钮 seek、主动 mark，并保留数量截断信息；请求和事件不等于确认听完。主观置信不自动转换成毫秒误差，也不直接作为生产采用许可。文本字段使用 textContent/输入值，无附件脚本执行。

选择人工原音定位是因当前证据缺口：05Q 新观察仍来自同一模型，无法独立证明前部声音类别或首词真实起点。没有用波形形状代替听觉判断，也没有将旧用户粗略举例当成精确标签。沿用 FK-PIT-0111/0112 的听感范围与对齐证据区分。

## 验证

32 项合同测试通过，覆盖完整回读、无法判断的空起点、媒体/批次/目标/schema 绑定、禁止自动验收、无答案/无标记、非有限/小数/越界时间、文本限额、各类日志错误及波形/范围错误。QA 下载经同一合同严格回读，并另核对 request/manual 两种 seek 来源均被保存、reviewId 重新计算一致。

实际独立无头 Chrome 检查初始未填、无标记禁用、缺答案不导出、点击波形、输入、微调、暂停处标记、无效输入恢复、三种区间播放、草稿恢复、清空和不确定导出。播放请求为 4000～9660、7100～9660、6800～9660 ms，4 倍速仅用于 QA；最终实测暂停点约 9747/9796/9694 ms，浏览器有少量停止延迟，不宣称精准裁切到请求终点。全部检查通过、页面错误为零。1100px 桌面及 390px 窄屏截图均人工查看，无横向溢出。标记 6500 ms 及示例答案仅为 Synthetic QA，不是用户听感。

QA 浏览器已关闭，独立进程标记扫描为零。三份 NAS 源文件 SHA/大小/mtime 保持，原音与旧附件未改。私有 review.html、review-data.json、verification、review-ui-report、截图及 qa-download/qa-uncertain 保存在 test-results/subtitle-quality-review/phase12/onset-listening。没有新增模型副本或本地服务。

旧 Markdown 规格沿用原台账，手工/私有检查本批需求引用、依赖、6.28 设计、记录与实现文件；不运行不兼容旧格式的 v2 spec.json 检查器。未改主进程/正式 UI，不重跑应用构建或宣称默认效果新增。代码与文档尚未提交。

## 等待人工结果

页面交付完成，任务仍为待人工听校。用户下载 asmr-onset-listening-annotations.json 后须核对上述 reviewId、源 hash、目标、有限字段及范围，完整保留备注和不确定。人工起点只作为独立定位线索，与 05Q 全部观察比较后再设计生产证据合同，不把这一私有时间硬编码进默认流程。

## 人工结果导入（2026-09-06）

用户附件 asmr-onset-listening-annotations.json 导出于 2026-09-06T10:43:56.037Z，SHA256 为 b91deb4fb980a8123e06891820f1ebb13805a0d5bebdc1983ef6b117aa2dd016。重新计算 manifest reviewId、核对音频 SHA，并通过现有 validateOnsetAnnotations 的 schema/目标/母媒体/音频/批次/枚举/时间/日志检查。附件按原始字节归档为 onset-listening/user-listening-annotations.json，校验及比较另存 user-listening-validation.json；没有改写下载原件或 NAS。

实际结果：estimatedOnsetMs=7380，confidence=clear，preceding=none，note 为空。none 的 UI 文案是“没有明显声音”，只记录这一主观判断，不扩展为波形零能量、所有呼吸都不存在或其他媒体语气词可删。clear 不等于毫秒级精度。记录中有 51 个事件、没有日志截断，主动标记依次 7380/7330/7380 ms，并存在自由拖动；最终答案使用导出字段的 7380，不用日志均值覆盖。播放/暂停记录仍不是听完证明。

| 观察 | 私 DTW 点（ms） | 相对人工标记（ms） |
| --- | --- | --- |
| 旧正式 0～30 秒 | 7400 | +20 |
| 原 0～18 秒及本次复现 | 7560 | +180 |
| 相同内容前补 1000 ms | 7480 | +100 |
| 相同内容前补 2000 ms | 7440 | +60 |
| 旧正式 4～18 秒 | 6360 | -1020 |

旧非同配置 6～20 秒为 7480 ms（+100），8～17 秒不含目标，保留但不计作同配置支撑。旧 0～30 秒另有 19300 ms 的“私が…”：这是不同发言的同字 token，完整保留但不参与本句误差比较。上述差值只是相对主观标记，不是经精密标注得出的模型误差基准。

这份独立反馈支持把 6360 ms 视为本例明显偏早的候选，并否定“因为新三份都输出了两声ん，所以完整前文一定正确”的推论；不单独证明模型内部具体对齐机制。下一步应围绕保留主文的短首句定位、对额外前文的独立约束和异常首 token 的拒绝设计通用规则。不能直接将 7380 ms 写进生产，也不将本例标签扩大为删掉任意语气声的规则。

T-SEG-05R 的人工定位任务已完成。此次只执行附件绑定/回读/比较及文档更新，没有修改生产代码、新增推理或再次索取相同听校。文件归档和参考比较通过；没有重跑或冒用上一轮的 32 项页面合同测试。05Q/05R 工作仍未提交。

## 用户补充澄清：两声清嗓声与后句之间有停顿

用户明确说明：前面是说话前清嗓子的两声“吭吭/咳咳/嗯嗯”，距离“私だ”还有一定间隔，不是连起来的，因此模型输出两声“ん”不能说完全错。此前将附件 none 进一步解释为前部声音不可靠的措辞过重，现予纠正：这两声有实际声音依据，存在判断以本条自然语言澄清为准；它们与后面的正式台词是分开的事件。

原始附件的全部字段和 SHA 保持，澄清单独保存在 onset-listening/user-clarification.json，绑定同一 reviewId 与附件 SHA；导入报告附上澄清并更新有效解释，不伪改原 JSON 为另一枚举。用户没有修改 7380 ms 主观起点，也未指定清嗓声的精确起止、停顿毫秒数或偏好的字幕写法，均不代填。

本例后续重点是独立清嗓声、停顿和“私だ”的来源/时间分配：不将清嗓声判成幻觉删除，也不因为它们存在就认可与台词连写或把词首提前。旧 6360 ms 仍比人工词首早约一秒；可能涉及首 token 吸收前部声音/停顿的解释继续作为待验证机制，而非已证实内部原因。此次只修正证据解释和后续设计，未修改生产行为。
