# T-SEG-03D：完整局部段组的短句时间复核

## 固定计划（推理前）

2026-09-06，阶段实现已提交为 `dd03f17`，按用户要求继续工作。本轮依据 04A 已验证的完整局部段组映射，检查第一父段中后部短句的时间资格；不重复此前第二父段“私”起点失败试验。关联 R-SEG-02/03/04/07。

保存的 0～55 秒与 0～30 秒非 VAD 观察均在 11.40 秒开始一个独立问句，两侧相邻词有效；但它们共享窗口起点，不能当作独立原音确认。固定增加两个裁窗：8～17 秒、6～20 秒；从工作区已有原音 PCM 副本按采样点裁取，不增益、不伸缩。各使用全新原生进程，仅一次请求，沿用同模型、CUDA、日语、beam=5、temperature=0、非 VAD、token_timestamps=true。失败只记录，不追加窗口寻找吻合结果。

本轮最多三项串行工作：保存上述两份观察；将完整连续候选段组的局部精确映射与词边界审计固化成离线工具及反例；给出可复核结果和下一步决定。不同观察的全部差异必须保留。任何通过仍只代表可提交听校，不能自动解除 T-SEG-04 的生产时间门。

资格要求：所有候选组文字必须按顺序唯一映射到原始父段，允许仅插入标点；禁止从有错词的原生段内抽取相似子串。目标边界必须位于完整匹配组内部。分别记录原生段界和相邻词区间，不把标点自带的时间当语音时间。零时长、长跨度、词串不覆盖候选文字、跨窗显著漂移均不得作为生产证据。局部裁窗将多句合为一个原生段时，可以报告内部词间隔供听校，但不得伪称其有原生段界。

所有私人响应、裁窗、哈希与试听页放在忽略目录 `test-results/subtitle-quality-review/phase12/local-timing/`。NAS 原件与其他模型只读，任务结束清理本轮进程。

## 结果

两次计划内请求均已保存，分别使用进程 6676、82540，且均已退出。首次脚本在响应落盘后记录耗时时发生变量错误；保留失败报告，只修复脚本并执行尚未运行的第二个裁窗，没有重跑第一份输入。第二次推理耗时 844 ms，加载 2653 ms；第一份加载 3159 ms，未恢复其不可核实的推理耗时。输入哈希复核一致。

新增离线 `local-word-boundary-audit.mjs`：完整局部段组唯一映射、组内词串覆盖、原音偏移换算、标点与词时间分离，以及所有观察逐项保留。它允许单原生段内的词边界参与审计，不将缺失的原生段界伪造出来；即使稳定也仅为词证据，`automaticAcceptance` 恒为 false。12 项新增测试和原局部证据 13 项测试通过。

| 原音观察 | 前词结束 | 后词开始 | 原生段界 |
| --- | --- | --- | --- |
| 0～55 秒 | 11.400 秒 | 11.620 秒 | 11.400 秒 |
| 0～30 秒 | 11.400 秒 | 11.620 秒 | 11.400 秒 |
| 8～17 秒 | 12.000 秒 | 12.160 秒 | 无，位于同段内 |
| 6～20 秒 | 11.650 秒 | 12.000 秒 | 无，位于同段内 |

四份均有完整且精确的局部文字映射，但前词端点漂移 600 ms、后词起点漂移 540 ms，超过事先设定的 300 ms，审计结果为 `window_disagreement`。不通过新时间资格；T-SEG-04 仍阻塞。普通 token 时间裁窗搜索到此停止，不选取吻合的长窗对子。

下一方向来自固定运行时实际尚未启用的 DTW 能力，单独建立 03E 范围，不把普通 token 时间失败扩大为所有对齐能力均失败。
