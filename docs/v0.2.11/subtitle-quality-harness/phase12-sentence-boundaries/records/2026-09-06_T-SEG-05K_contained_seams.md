# T-SEG-05K 包含与多父段接缝原音核对

2026-09-06，R-SEG-03/04/07，M 单模块串行，按设计 6.21 完成五次固定观察。此任务完成限定诊断、来源工具和实验听校材料，人工结果与默认修复尚未完成。

## 两处结论

102.5 秒：左原段“毛も耳の女の子がいるんだけど その子は魔力過剰症といって今日は”中的右短句重复并非纯后缀。原始末端多出的“今日は”不能仅凭包含关系删去。95～115 / 94～114 秒两次观察均为“ケモ耳…その子は魔力過剰症といって（另一份为と言って）強大な魔力を持っているがゆえの発作に苦しんでいるわ”。两份“強”首点均为 104740 ms；没有出现“今日は”不等于自动证明它未发音。本批该处字幕不变，听校页单独核对“今日は”是否真实存在。保留“毛も耳／ケモ耳”“といって／と言って”异文，以及窗口边缘“ヒマリっていう／ひまりを救ってあげてと”等额外内容，不假装所有文字都稳定。

127.5 秒：重复涉及左父段 cue-19 的字素 [11,15)“によって”、cue-20 全部 [0,13)“相手から魔力を吸収する能力”，以及右父段 cue-21 的 [0,17)。来源工具保留两条左文与右条 [17,57) 全文，明确标注被重复覆盖的右前缀。它只输出 source_accounting_only 和未决条件，不授权去重或时间移动。

120～145 / 119～144 两份观察中，来源合成的完整正文各只出现一次（去标点/空白的诊断比较）；第二份含完整引号结构，原始响应和报告均保留，并没有放宽已有默认引号护栏。下一句“エッチの知識だけは豊富なあなたならあの子のこときっと救えるわよ”的首点为 133200 / 133240 ms。首句“あ”首点 122220 / 123260 ms 相差 1040 ms，故不宣称首句起点准确、不择优换窗。

## 实验字幕与范围

仅供人工听校的实验将原三条整理成两条：121980～133200 ms“あなたにはエッチな行為によって相手から魔力を吸収する能力を与えておいたわ”；133200～143440 ms 保留完整下一句。外侧起止不变，只去掉已记录的重复右前缀；整轨 31→30 条，其他 28 条保持，已上线 52.5 秒修复保持。SRT/LRC 经生产格式器导出并严格读回，字词及起点一致。此为有来源记录的人工试验，不是机器自动资格通过或实际新默认输出。

听校页同时提供 96～110 秒原音核对和 120～144 秒实验对比。用户需分别判断第一处“今日は”、第二处去重内容及 133200 ms 换句时间；可选不确定和补充说明。不会要求第一处把未确认词先删掉再验收。文件为 asmr-contained-seam-listening-annotations.json，schema fusionkit-contained-seam-listening-v1。

## 实现及验证

新增 benchmark/contained-seam-source-audit.mjs 与测试，记录包含之外全部文字、跨父段原字素范围、来源时间重叠与重复字符串歧义。9 项 Node 测试通过。新增判断只在 benchmark 使用，没有修改默认执行器；没有重新运行上一阶段 231 项或完整应用六素材并冒充本轮证据。

页面验证通过：原音时长 175.993 秒；128 秒旧字幕仍带重复，候选为合成首句；133.3 秒显示完整下一句；两个按钮起止有效、下载 schema/媒体/候选字段正确、默认均未判断、窄屏无横向溢出。桌面及窄屏截图已目视检查。自动 QA 静音四倍速运行，停止于 110.528 / 144.067 秒；qa-download.json 为合成答案，不是用户验收。浏览器已关闭。

五个新进程均已退出：85776、72404、13852、87784、40460。所有响应保存在 contained-seam-audit，未新增第六次识别。B-noise 仍输出九次“はい”，全部保留，不宣称非语音识别准确。原音 SHA 前后相同，NAS 未写入。运行模型与服务器 SHA 与 05I 相同，完整字段/输入哈希见 A-report.json。

## 后续

等待本听校包的两个问题反馈，再分别制定含异文的额外词处理和多父段完整句规则。默认 7 根窗主请求 12 次加已验收前缀两次已用到 14，未来不能直接再塞入这些观察，也不能撤掉已有成果来腾预算，须单独设计可复用证据与可选预算合同。T-SEG-05 仍未完成；本轮无提交/推送。

媒体 SHA：`9c80350769700f72edc5bd7e7428122f42b1c8eede81967dbe723e5fb7c4fc10`。
reviewId：`db1bb58de36ea59e7b95a6ee8255050d166da3c9d404d865076eb1bd6ce3fe55`。

## 2026-09-06 人工结果回收（更新上述待听校状态）

用户附件导出于 `2026-09-06T07:27:16.494Z`。第一处 middle.judgment 为 `absent`：没有单独的“今日は”，无补充说明。第二处内容 `resolved`、时间 `aligned`，对应 133200～143440 ms 下一句，无补充说明。schema、媒体 SHA、reviewId、条目数、短语及候选起止均与听校包一致，重新计算 review-data 内容摘要也匹配。

附件 SHA-256：`53ecc5ba9512a15fbb791eb83f592d5396bc23125233a4a8ad0d65a7c8946e3c`。原附件逐字节归档于私有目录 `contained-seam-audit/user-listening-annotations.json`，验证报告 `user-listening-validation.json`，脚本 `test-results/phase12-contained-listening-validation.mjs`；未使用合成 QA 答案。

已确认的范围：102.5 秒附近“今日は”不属于单独真实台词，可作为本样本额外误识别的人工标签；127.5 秒多父段合成实验的内容及 133200 ms 换句已通过。第一处此前没有提供修改后的字幕，因此尚无该处新分句/时间结果的人工验收；首句 121980 ms 是保留的旧起点，不把此次 aligned 扩展为其毫秒级起点证明。

下一步优先完成已通过的多父段规则之生产资格和请求预算设计：仍需处理完整来源覆盖、候选引号结构、真实重说反例及原预算已满的情况，不能把本样本正文/时间写死，也不能省略必要观察。随后针对已确认的“今日は”误识别构造保持前后文的修复，并验证新的分句与时间。相同多父段候选若在默认流程复现，无需重复听校。

本次只回收和验证人工结果、更新任务记录，没有新增推理、修改默认输出或写入 NAS；以上两处尚未接入生产。
