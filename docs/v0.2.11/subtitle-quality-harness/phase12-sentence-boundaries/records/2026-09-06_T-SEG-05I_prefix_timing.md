# T-SEG-05I：前后缀重复的后文时间

2026-09-06，关联 R-SEG-03/04/07，沿用 M 单模块串行。按设计 6.19 的三次固定观察核对时间；先完成实验资格，不直接放宽生产去重间隔或请求数量。已获连续推进授权，文档先行后执行。

## 结果与范围

独立音轨 52.5 秒接缝获得实验候选：前条 45570～51720 ms 保持；后条删除重复前缀 `のテクノブレイク `，由 52500～61230 ms 改为 55880～61230 ms，完整保留后文 `さすがにショック大きいみたいね`。其他 30 条完全保持，总数仍为 31。SRT/LRC 导出并严格读回通过（LRC 仅按格式规则将段内换行转为空格）。这不是实际应用的新输出，默认流程尚未接入。

## 观察与实现

使用原音 45～65 秒、44～64 秒两个固定窗口，各在新进程运行一次无条件原音 DTW；第三次固定为 B-noise 负对照。后半句首点为 55880 / 56000 ms，相差 120 ms；重复前缀末点为 51500 / 51520 ms。直接采用事先指定首个观察的 55880 ms，没有平均分字时间。两次完整前缀/后文都唯一对应，起点处都有原生段边界。

前缀末点到后文首点相隔 4380 / 4480 ms，超过旧核心 2500 ms 上限。本次新增独立离线审计器，显式标记 longGap，仅提出听校候选，没有放宽生产核心；这些点不能证明中间都是静音。两次观察中前文 `オナーニー` / `オナニー` 的差异保留原响应，没有替换正文。B-noise 仍识别出九次 `はい`，不能宣称无声识别已修复；其响应作为对抗性替换输入时因缺少完整对应而被拒绝。

新增 `scripts/local-subtitle/benchmark/prefix-overlap-timing-audit.mjs`：验证来源重叠、完整唯一前后缀、词边界、完整 token 覆盖、单调且有界原音时间；所有观察都参与，任一矛盾即拒绝。最少两种不同起点，时间分歧上限 300 ms，输出始终 `automaticAcceptance: false`。有界请求合同见设计 6.19，目前只完成设计，未改变默认预算。

## 验证与私有产物

- 离线审计 13 项测试通过，覆盖矛盾第三观察、重复来源、不同媒体、文字差异、重复、缺词、token 内部、缺句界、逆序、越界、超过 8 秒间隔、来源与引号结构。
- 私有产物在 `test-results/subtitle-quality-review/phase12/prefix-overlap-timing/`：三份原始响应、A-report、audit、实验 SRT/LRC、review-data、review.html、桌面/窄屏截图、review-ui-report、cleanup-report。
- 浏览器功能检查通过：53 秒候选尚未显示，56.1 秒显示完整后文；48～63 秒按钮在 63.062 秒停止，下载字段正确，窄屏无横向溢出。自动化为静音功能检查，`qa-download.json` 是合成答案，不能计作人工验收。截图已人工目视检查布局。
- 三个探针进程 50404 / 44676 / 77944 均已退出，浏览器测试进程已关闭；工作区原音 SHA-256 前后相同，NAS 文件未写入。

媒体 SHA-256：`9c80350769700f72edc5bd7e7428122f42b1c8eede81967dbe723e5fb7c4fc10`。
模型 SHA-256：`64d182b440b98d5203c4f9bd541544d84c605196c4f7b845dfa11fb23594d1e2`。
服务 SHA-256：`c72f99b75ae4111d6e7ddb62e04d3c0c94fea2ba6536412e517f256b4388dd37`。
听校 reviewId：`46953ea4ac186680d39480d5f34f25b873f8011273bdae69f347e7c10f589d53`。

## 原听校检查点（已完成）

请用户在一个 48～63 秒片段分别判断重复内容和后文出现时间，导出 `asmr-prefix-overlap-listening-annotations.json`（schema `fusionkit-prefix-overlap-listening-v1`）。核对反馈后再安排生产接线及六素材回归，之后处理 102.5 / 127.5 秒的中部包含和多父段来源。T-SEG-05I 完成的是有界实验资格、预算合同与听校包，人工结果、默认启用及其余接缝仍未完成。

## 2026-09-06 用户结果回收

用户附件导出于 `2026-09-06T05:59:00.599Z`，唯一条目 `さすがにショック大きいみたいね` / 55880～61230 ms，内容 `resolved`、时间 `aligned`，无补充备注。媒体 SHA、reviewId、短语、起止、条目数及 schema 均与原听校包一致；重新计算 review-data 除 reviewId 外的 JSON 摘要也匹配。附件 SHA-256 为 `3f68becbf3b9fda4a94a8a22c7d817b9fa79a34ee7231f02d5e07d6261407840`。

原附件逐字节归档至私有目录 `prefix-overlap-timing/user-listening-annotations.json`，结构及身份验证报告为 `user-listening-validation.json`，验证脚本为 `test-results/phase12-prefix-listening-validation.mjs`。未使用 qa-download 的合成结果。本候选内容及时间现已通过人工验收；不把这一条通过扩展为整轨或所有素材已修复，不把听感 aligned 解释成毫秒级边界测量。

下一步为默认生产实现。复核 production-executor.ts 后确认：旧接缝只允许相邻根窗各一次主请求、共享一个可选请求；本例相邻根窗均有一次条件化失败后的原音回退，且新资格要求两份不同起点的 DTW 观察。6.19 的一次补充预算草案不足以直接承载这项规则。生产设计必须为一处接缝成组预留两次观察（可分别计入相邻根窗），在旧可选流程之后校验总数剩余额度 ≥2、每个根窗仍无可选请求且只经历允许的主恢复路径，任一观察失败或额度不足都保留原字幕。独立素材原主请求 12 / 根窗 7，仅在旧可选流程未占用剩余额度时才可容纳两次观察至 14；需用实际执行记录核实，不能仅凭本清单宣称符合预算。

这次仅完成反馈归档、人工验收状态与接线前约束核对，默认输出没有改变；没有新增推理或写入 NAS。后续接线必须补充成组预算、失败/取消、原生状态隔离测试，以及六素材实际导出对照；新实际输出若重现同一已验收候选，不要求用户重复听校。
