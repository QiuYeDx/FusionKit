# T-WORKSPACE-04 恢复、取消与旧结果保护

| 字段 | 值 |
| --- | --- |
| 任务 | T-WORKSPACE-04 |
| 日期 | 2026-09-10 |
| 验证版本 | 基线 9c88565 加未提交工作树；源码摘要 14d442b718cfb028fffbd3148c85c9c48ffa73714d0bad4b5c34eb235b609bd5 |
| 环境 | macOS arm64；既有 pnpm 8.7.0 node_modules；Vitest 2.1.9、Vite 5.4.21、Electron 41.10.6、Playwright 1.58.2；未安装依赖或修改 lockfile |
| 任务指纹 | 36bbe78b17e6084c8992faf08fa23a13dec6da75c83a3b8e502879eef8a4a5c6 |

## 实际结果

用户要求“好，看一下接下来该做什么了，没问题的话就开始做吧，往后推进工作”。已核对 T03/T07 集成于干净基线 9c88565，在已批准 I1 内继续 T04；真实授权登记在 spec.json，不开启 I2/I3/I4。

任务检查点保存冻结批次成员、上下文、预算、源摘要和期望轨修订。启动将遗留运行任务持久转为 interrupted，用户明确继续时重建相同短 ID/批次，只恢复未提交部分。模型身份与原配置一致才允许继续；密钥可轮换，不写入检查点。旧无清单任务保留结果，提示创建新翻译。

取消先提交状态与 generation，再中断请求；已提交译文保留，迟到响应不能写回。应用级 FIFO 限制两条真实供应商请求，不合作的请求结束前仍占名额。单层有限退避保留 Retry-After 下限。请求结果未知的累计记录在后续成功后仍保留；取消事务排队期间已收到的用量按实际记录。并发 dispose 共用关闭 Promise，拒绝新任务并对关闭时修订竞争进行有限重读。

恢复和取消使用新精确 IPC，保留 owner/frame/grant/revision 校验。renderer 只接收检查点版本标记，不接收完整冻结正文和成员清单。UI 沿用紧凑状态栏和 ScrollableDialog，按所选译轨显示继续/取消、原模型配置修复、等待倒计时和未知计费提示。

## 验证结果

| 验收 | 结果 | 关联检查 |
| --- | --- | --- |
| AC-WORKSPACE-05-1 | 通过 | V-WORKSPACE-04-1, V-WORKSPACE-04-3 |
| AC-WORKSPACE-05-2 | 通过 | V-WORKSPACE-04-2, V-WORKSPACE-04-3 |

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| V-WORKSPACE-04-1 | 通过 | inline: recovery 24 项与 service 35 项通过；真实 Electron 首批提交、第二批在途时 SIGKILL 主进程，跳过 dispose，原 userData 重启后首批保留且没有新请求；修复原配置并轮换 key 后仅调用第二、三批。全链路请求共 4 次，首批内容未重发，原 taskId 与首批 entry 保持一致 |
| V-WORKSPACE-04-2 | 通过 | inline: 125 项针对性回归通过（recovery 24、service 35、IPC 10、checkpoint 11、repository 21、bilingual-service 24）；覆盖源/轨/generation 冲突、重复启动、跨文档、非合作网络物理并发、排队取消、Retry-After、发布后异常、未知用量和关闭竞争 |
| V-WORKSPACE-04-3 | 通过 | inline: recovery-ui 最终 36.78s 通过；缺失/修改原 profile 不给恢复，原 profile 修复后继续，取消后迟到响应无写入，31 秒供应商期限禁用提前继续，旧检查点提示重新翻译；既有 translation-ui 和 lifecycle-ui 各通过。桌面 1280×860、窄窗 786×540、深浅主题最终截图均实际审阅 |

```sh
node_modules/.bin/vitest run test/subtitle-studio/translation-recovery.test.ts test/subtitle-studio/translation-service.test.ts test/subtitle-studio/ipc.test.ts test/subtitle-studio/translation-checkpoint.test.ts test/subtitle-studio/repository.test.ts test/subtitle-studio/bilingual-service.test.ts --maxWorkers=1 --minWorkers=1
node_modules/.bin/tsc --noEmit
node_modules/.bin/tsc --noEmit --moduleResolution bundler --module esnext
node scripts/subtitle-studio/check-boundaries.mjs
node scripts/check-i18n.mjs
node scripts/check-i18n-usage.mjs
# 在隔离源码副本执行，使用现有 node_modules
node_modules/.bin/vite build --mode=test
node scripts/check-preload-bundle.mjs
FUSIONKIT_STUDIO_E2E=1 node_modules/.bin/vitest run test/subtitle-studio/translation-recovery-ui.test.ts test/subtitle-studio/translation-ui.test.ts test/subtitle-studio/lifecycle-ui.test.ts --maxWorkers=1 --minWorkers=1
```

构建、preload、bundler 模式 TypeScript、边界检查（101 文件/0 错误）、四语言完整性（各 139 studio 键）、diff/spec ready/approval 检查通过。源码摘要以 git ls-files -m -o --exclude-standard 去重排序，排除 docs/，依次累积路径、NUL、字节、NUL，共 21 文件；最终生产文件与测试已和构建副本逐字节比较一致。

## 渲染与测试修复

参照 qiuye-ui-quality、项目 UI 与避坑 Skill，实际审阅 running-before-restart、missing-original-model、resume-dark-narrow、resumed-completed-narrow、cancelled-desktop、provider-wait 六张图。弹窗内部信息分组清楚，原模型/批次和操作对齐，长修复说明可换行；窄窗恢复说明与按钮可达。背景表格保持当前密度与内部滚动，没有新增固定大留白。

第一轮 recovery-ui 在强杀前直接修改 localStorage 后，Chromium 尚未落盘，重启仍读到原 profile，导致“配置缺失”断言失败。调整测试为先独立验证真实崩溃恢复和无新请求，再重启后删除配置并 reload；生产代码未为测试改动，最终完整流程通过。其他两条 Electron 回归首次通过。

截图和构建日志保存在本机忽略目录 test-results/subtitle-studio-recovery/。使用 /private/tmp/fusionkit-studio-recovery-foeyvrpo 构建，避免影响用户 dev 的 dist-electron；模拟 HTTP 和测试应用在 finally 关闭，未启动 Vite dev 服务。未调用真实 API，也未读取/上传用户字幕。

## 风险与未执行项

未知供应商执行不能保证无重复计费，界面明确提示；旧 T03 无冻结清单的任务不猜测恢复。未执行 Windows 平台验证，本项 macOS 恢复证据不代表 Windows 原生文件替换验收。导出完整模式归 T05，I1 整体验收归 T06。

默认 tsc 仍为既有 smooth-corners/observer 模块解析 TS2307；补充 bundler 模式通过。i18n usage 仍为既有 NameTranslator OptionsPanel 动态 key 与 stale manifest 两项，字幕工作台无新增错误；构建有既有 chunk/混合引用警告。上述未伪装为全部全局检查通过。
