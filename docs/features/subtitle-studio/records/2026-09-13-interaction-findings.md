# I6 第三轮交互反馈

2026-09-13用户七图与十项请求。基线3855c2c加I5未提交工作树；核对上一轮快照65文件字节一致。本轮没有提交请求，保留全部已有改动。

1. **队列进度对比**：浅深色hover时进度槽与行背景保持可见差异，0/部分/完成进度可辨。 保留停止按钮、键盘和取消能力，不以去掉行hover绕过问题。
2. **自动翻译提示密度**：自动翻译为元数据行短标签，完整说明按需显示，不独占进度后的整行。 长名窄窗合理收缩换行，不遮挡状态/百分比/操作，错误仍可发现。
3. **明确复制语义**：复制按钮打开菜单，明确选择原文、译文、双语及带时间信息，纯文本选项不暗含时间。 缺失/过期译文及未知结束时间按实际状态显示，不猜内容和结束时间；键盘、实际剪贴板和反馈正确。
4. **已选文档次序**：导出已选文档位于编码与换行之前，默认折叠，多轨才显示选择。 共享编号与名称首行对齐，长名/操作/边缘渐变保留。
5. **导出明确确认步骤**：检查后进入新标题的独立确认内容，只显示就绪/受阻数量、输出概况及合并变化摘要，文件详情默认折叠。 明确确认按钮接受所示变化并实际导出，返回设置/取消/部分就绪/全部受阻/修订过期均可理解，保留真实plan和acceptedLosses，不在设置内偷偷追加勾选。
6. **简洁结果按需详情**：导出/原始下载/批量翻译/导入/管理结果先显示简洁成功、失败或跳过总览，清单默认折叠。 失败详情和恢复入口保留；空结果/部分失败/长列表/键盘可用，不把实时任务监控当成结果隐藏。
7. **面板标题密度**：同类面板header适度减少竖直留白，保留字号、操作热区与靠角协调。 只改工作台，宽窄浅深及长短状态可读，不全局压缩其他工具。
8. **视图切换稳定**：文档/转写共享标题及顶部空间基准，来回切换标题和工作区顶部不大幅跳动。 处理真实滚动容器，不丢任务/选择，窄窗流程和底部导航可达。
9. **文档侧栏任务概览**：翻译总览只显示在文档视图，宽窗置左侧独立紧凑面板，转写视图不显示。 活动/完成/失败及最近批次真实，查看全部可达；窄窗入口归属文档区域且不挤压正文。
10. **全选局部更新**：全选/清除不触发全页busy、读取状态、无关禁用或正文重读，当前已有页面选择立即响应。 跨页/筛选/100上限保留，远程范围查询仅局部pending；重复操作/查询变化/删除/卸载不回写旧选择。

图1对应1/2，图2对应3，图3对应4，图4对应5，图5及类似场景对应6，图6对应7，图7对应9；8/10为用户切换和选择体验。图片中的真实媒体标题不复制到合成测试。

沿用连续实施授权，记录后完成十项及必要隔离验证。打包和I4继续后置，不改ASR算法，不代签用户验收。
