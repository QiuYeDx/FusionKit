# T-WORKSPACE-06 I1 共存与移除集成验证

| 字段 | 值 |
| --- | --- |
| 任务 | T-WORKSPACE-06 |
| 日期 | 2026-09-10 |
| 验证版本 | 基线 9c88565276349955d23df1b926092634346f6413 加共享工作树；源码摘要 03c64252876caeb2d4cb0e0e2592f74c94d252190c51fe30debf9f688cbd6ec8 |
| 环境 | macOS arm64；既有 pnpm 8.7.0 node_modules；Vitest 2.1.9、Vite 5.4.21、Electron 41.10.6；未安装依赖或改动锁文件 |
| 任务指纹 | 9ce6ad1140e37139a1e1c0de7ced91a85787a104c41ff8f37529800e821ef1cd |

## 实际结果

在 T04/T05 完成后按相同 I1 授权推进集成验证。新增共存/独立 Electron 场景、可复现的隔离移除脚本，并将实际旧翻译测试目录和应用组合文件补入边界清单；共享 Boolean consumer 测试登记新版双语与导出的显式确认控件。未修改根工作树旧工具实现，没有启动 I2。

共存和移除构建分别使用新的隔离 userData、合成 SRT、原生选择/保存对话框替身和本机 HTTP 模型 fixture。新版实际主进程发送三批请求，重启后核对整个文档/任务快照，完成 12 种 mode/format/order 组合并解码、parse-back 校验正文与时间；模型调用始终为 3。原始字节导出不变，移走合成源文件与模拟旧数据/模型目录后再启动，仍能原文件和纯译文导出。删除新版文档后 15 份输出保留，renderer/server 错误均为零。模拟旧文件在移走前逐字节不变；这不是对真实用户目录执行删除。

脚本 `scripts/subtitle-studio/prepare-removal-rehearsal.py --build` 只创建临时源码副本，移除 v1 翻译、转写、状态、类型、测试、资源及应用/IPC/Agent/builder 注册贡献。公共音频和文件选择借用的完整 Windows Explorer 路径解析器仅在演练副本复制到公共 fs，保留原算法和对应测试，不留下 v1 服务或资源依赖。移除前后的摘要、全部路径及构建命令写入 manifest；root Vite 配置在副本全新构建 renderer/main/preload。根仓库 v1 文件未删除。

最终移除清单记录 1114 个复制文件、411 个移除项、29 个修改项、3 个新增项，manifest SHA256 为 e8c6be99c514d3083025d08aede69f024013e9e8d8ba55b13187ef35a4022eb5。其中含一次 py_compile 产生的临时缓存快照，根仓库缓存已清理，不参与生产代码。新版生产文件与共存构建逐字节一致，新版自身文件与移除构建逐字节一致；应用组合变化按清单核验。最终 Electron 测试仅在构建后修正快照轮询：对 list/read 之间的 revision_conflict 最多重读 10 次，其他错误直接抛出；生产修订保护未放宽。

## 验证结果

| 验收 | 结果 | 关联检查 |
| --- | --- | --- |
| AC-WORKSPACE-01-1 | 待验证 | V-WORKSPACE-06-1 |
| AC-WORKSPACE-01-2 | 通过 | V-WORKSPACE-06-2 |
| AC-WORKSPACE-07-1 | 通过 | V-WORKSPACE-06-3 |

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| V-WORKSPACE-06-1 | 待验证 | inline: 旧工具相关 41 文件/625 项中 620 通过、5 失败；同名失败在未修改 HEAD 的三文件 141 项中再次复现。Electron 旧字幕真实 File 导入、HTTP 失败/重试、空目录恢复扫描、旧转写页面与 ja/initialPrompt 偏好前后 JSON 比对通过；未运行真实 ASR 或含历史任务的原生恢复 |
| V-WORKSPACE-06-2 | 通过 | inline: 边界 112 文件/0 错误，正反例 2 项通过；实际删除副本全新构建、类型/bundle 检查通过，src/electron 旧 v1 路径/API/类名扫描 0 匹配，旧两项 preload API 为 undefined；共存 14.30s 和移除 13.56s Electron 链路通过，三次模型请求后重启/12 组合导出均无新请求，缺旧数据与源文件仍成功 |
| V-WORKSPACE-06-3 | 通过 | inline: i1-coexistence-ui 38.79s 通过；四语言恢复/导出、实际失败任务、空文档诊断、101 条长文、键盘翻页和焦点；结合 T04/T05 深浅主题和 786×540 窄窗、取消/缺配置/处理中场景。加载遮罩退出后截图，最终截图实际审阅 |
| V-WORKSPACE-06-4 | 失败 | inline: 默认 tsc TS2307（smooth-corners/observer），i18n usage 的 NameTranslator 动态 key/stale manifest 为既有失败；bundler 解析 tsc、隔离 Vite 全新构建、preload、四语言各 185 studio 键、边界、diff/spec 检查通过 |
| V-WORKSPACE-06-5 | 失败 | inline: 新版全套 313 通过/12 opt-in 跳过，新增两项 Electron 另行显式运行通过；旧回归 620 通过/5 基线失败，移除副本保留工具 83 通过/4 Windows 场景跳过。各测试 finally 关闭 Electron/HTTP，旧测试进程组确认退出，最终 ps 无本轮 Vite/Electron/测试进程 |

```sh
node_modules/.bin/vitest run test/subtitle-studio --maxWorkers=1 --minWorkers=1
node_modules/.bin/vitest run test/subtitle-studio/boundaries.test.ts src/pages/Tools/_shared/ui/toolBooleanControlConsumers.test.ts --maxWorkers=1 --minWorkers=1
node_modules/.bin/tsc --noEmit --moduleResolution bundler --module esnext
node scripts/subtitle-studio/check-boundaries.mjs
python3 scripts/subtitle-studio/prepare-removal-rehearsal.py --build
# 以下仅在相应全新构建的隔离源码副本内执行
FUSIONKIT_STUDIO_E2E=1 node_modules/.bin/vitest run test/subtitle-studio/i1-coexistence-ui.test.ts --maxWorkers=1 --minWorkers=1
FUSIONKIT_STUDIO_E2E=1 node_modules/.bin/vitest run test/subtitle-studio/i1-independence-ui.test.ts --maxWorkers=1 --minWorkers=1
FUSIONKIT_STUDIO_E2E=1 FUSIONKIT_STUDIO_REMOVED=1 node_modules/.bin/vitest run test/subtitle-studio/i1-independence-ui.test.ts --maxWorkers=1 --minWorkers=1
```

根本地套件在加入最后两个 opt-in E2E 文件前运行；不将跳过计为通过。两项新增场景随后显式运行，共存场景一次，独立场景分别在共存/移除构建运行。边界与共享控件最终 4 项通过，bundler TypeScript 最终复查通过。

旧版五个失败位于 jobManager.test.ts:3143、jobManagerIpc.test.ts:141/247/327、subtitleExporter.test.ts:203，断言整个返回值不得包含 fixtureRoot，但已有公开结果含 inputPathDisplay/outputPathDisplay。相关旧代码与测试本轮无差异；没有删除断言或替旧行为背书。默认 tsc 的模块解析失败及 i18n usage 的 OptionsPanel.tsx:117 DYNAMIC_KEY/STALE_MANIFEST 延续 T02/T03 既有结果。

## 渲染与证据

实际查看旧翻译失败/重试、旧转写无资源、日语/繁中导出、英文恢复以及共存/移除版翻译完成截图。原译文列和完整时间可读，状态与未复核标记明确；弹窗长文案、可见策略和底部操作无截失。其余恢复/导出窄窗主题证据见 T04/T05 记录，不重复制造 UI 改动。

最终证据取回忽略目录 `test-results/subtitle-studio-i1-final/`：两构建 result.json/截图、四语言截图、移除 manifest/五份检查日志、Electron 日志、新版全套日志和最终源码比对。旧回归/HEAD 对照的完整命令与 JSON 日志在 `test-results/subtitle-studio-i1-coexistence/`。源码摘要按 git ls-files 的修改/未跟踪文件去重排序、排除 docs、累积路径/NUL/字节/NUL，共 36 文件。截图和临时构建不进入 Git；可用仓库测试和脚本重新生成。

## 风险与未执行项

T06 保持待验证，I1 acceptance 保持 pending。补齐顺序：在有已配置模型和限定音频的隔离环境运行旧版真实转写/历史恢复；确认并解决或明确处置上述既有检查失败；重跑受影响 required 检查，再请用户验收 I1。不能用 HTTP fixture 或空恢复目录冒充真实 ASR/历史任务验收。

未执行 Windows 实机或发布包验证，Windows 原生资源和转写继承属于后续 I2；T05 发布锁定失败仅为故障注入。移除演练证明 I1 运行不需要旧实现/旧数据，未声称完成系统级文件读取追踪或原生包独立性。未调用真实供应商 API，未读取私人字幕/音频或用户偏好。所有构建在临时副本完成，避免根 Vite 清理用户 dist-electron；本轮启动进程已结束，未安装依赖或更新 pnpm-lock。
