# 实施记录：T-WORKSPACE-02

| 字段 | 值 |
| --- | --- |
| 任务 | T-WORKSPACE-02 |
| 日期 | 2026-09-08 |
| 验证版本 | 基线 a8e2eea72ea08c6ed40e684fbbc9c2de3e529774，非 docs 改动 20 文件 SHA-256：063275e6ea74b58999d6e1e7fccb069b3872b4ff69e2b65766f3873d55ceea6f |
| 环境 | macOS；沿用本地 pnpm 8.7.0 依赖，不更改 lockfile |
| 任务指纹 | df342d6383eda73738a88a20e30b7fe7af97db71eea13ba8c09ba1b500856e97 |

## 实际结果

用户在当前任务明确回复“好，继续推进工作吧”，恢复已批准 I1 开发。完成 T-WORKSPACE-02，保留 UI 优化，不涉及 I2 或实际 v1 删除。用户随后要求前端工作使用 qiuye-ui-quality；已读取该 Skill 及其 failure-catalog/rendered-review，结合 FusionKit UI Skill 执行渲染审阅。

实现 document/tasks 严格快照、跨实例串行提交、current/previous 指针和摘要验证；恢复只接受已发布 generation。列表按 UUID 目录重建。删除墓碑先于取消与自有目录清理，刷新或重启可重试残留清理；移除终态任务保留译文。目录/文件符号链接拒绝、尺寸检查、未知凭据字段拒绝均有测试。

生产 IPC 保留精确 allowlist，补齐 owner/main-frame/应用 URL/修订校验和发布前撤权检查；源导出与删除串行发布。新增固定事件订阅，前端保留分页外的修订观察和删除墓碑。四语言删除确认采用现有工具栏、图标按钮和 ScrollableDialog；不新增任务/翻译占位 UI。

## 验证结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| V-WORKSPACE-02-1 | 通过 | inline: repository.test.ts 覆盖 7 个写入/sync/发布故障点、当前指针或 generation 损坏、有效 JSON 篡改、旧指针兼容、索引损坏、孤立 generation 拒绝及跨实例修订竞争；lifecycle-ui.test.ts 实际 Electron 重启恢复 revision 2 与 b1 检查点，移走源文件后导出内容一致 |
| V-WORKSPACE-02-2 | 通过 | inline: repository.test.ts 覆盖清任务保留译文和导出、墓碑发布先于取消、清理失败与重启重试、迟到提交和同 ID 重建拒绝、撤权发布拒绝、外部目录/导出/v1 sentinel 保留、凭据字段拒绝；实际 Electron 删除后重启列表仍为空 |
| V-WORKSPACE-02-3 | 通过 | inline: ipc.test.ts 直接注册生产 handlers，注入 Electron adapter 验证跨 owner、伪造 sender/frame/token/path/revision/method、远程页面注册、导航/销毁撤权、保存对话框中删除、preload 私有信息隔离；observations.test.ts 覆盖跨页/旧列表/旧详情/删除墓碑；Electron 脚本字幕仅显示文本，window.studioInjected 未定义且无 pageerror |

## 命令与 UI 证据

源码摘要算法与 T01 相同：`git ls-files --modified --others --exclude-standard` 的非 docs 路径去重排序，逐项以路径、NUL、文件字节、NUL 输入 SHA-256。包括新增测试；文档变更不影响此摘要。

| 验收 | 结果 | 关联检查 |
| --- | --- | --- |
| AC-WORKSPACE-03-1 | 通过 | V-WORKSPACE-02-1 |
| AC-WORKSPACE-03-2 | 通过 | V-WORKSPACE-02-2 |
| AC-WORKSPACE-07-2 | 通过 | V-WORKSPACE-02-3 |

- `node_modules/.bin/vitest run test/subtitle-studio/{repository,ipc,observations,formats,boundaries}.test.ts`：最终 42/42 通过（17+5+1+17+2）。最初 IPC 断言误用当前 Vitest 不支持的 matcher，已改为兼容断言后通过。
- `FUSIONKIT_STUDIO_E2E=1 node_modules/.bin/vitest run test/subtitle-studio/lifecycle-ui.test.ts test/subtitle-studio/workspace-ui.test.ts --maxWorkers=1 --minWorkers=1`：最终 2/2 通过，48.25s；使用真实生产 main/preload/renderer，原生文件对话框返回值受控，隔离 userData。
- `electron.test.ts`：本轮两格式生产导入/重启/源导出回归 1/1 通过；不将受控对话框称作新的人工作业证据。用户此前手动导入和保存结论仍留在 T01 记录。
- 沙箱启动 Electron 首次 SIGABRT；通过宿主权限机制启动隔离实例后通过。既有 workspace UI 测试一次在长文件名 hover 断言失败，改为等待导入及事件刷新结束，并经过表头到文件名的真实 hover 路径后复验完整流程通过。
- UI 参照为本分支已优化的工作台与既有字幕转换器共享面板。沿用 ToolPanel、StudioIconButton（ghost/icon-sm）、ScrollableDialog 和 outline/destructive 按钮，不改页面 CSS 或共享组件。确认框完整显示长名称，取消/删除清楚区分；桌面及 786×540 无弹窗横向溢出，取消后焦点恢复。
- 首次截图处于淡入中间帧，审阅发现后补 `opacity=1` 等待与截图动画收敛。最终已查看 `test-results/subtitle-studio-lifecycle/delete-confirmation-desktop.png`、`delete-confirmation-narrow.png` 和 `test-results/subtitle-studio-ui/dark-en-1280.png`；此前半透明截图不作为通过证据。四语言、窄窗口、分页/复制/原始内容/错误重试等原有 UI 回归通过。
- `node_modules/.bin/vite build --mode=test`：renderer/main/preload 通过，保留既有 chunk 大小与混合 import 警告；`node scripts/check-preload-bundle.mjs` 通过。
- `node scripts/subtitle-studio/check-boundaries.mjs`：57 files / 0 errors。新增 ScrollableDialog 的传递依赖仅为公共 UI、Radix Dialog 和 Motion，阅读实际组件后登记既有包，无 v1 依赖。
- `node scripts/check-i18n.mjs`：四语言 studio 各 52 keys，完整性通过，17 项既有其他命名空间同文警告。
- `node_modules/.bin/tsc --noEmit`：既有 `src/components/qiuye-ui/smooth-corners.tsx:6` 的 observer 导出无法由旧 moduleResolution 解析；该文件与 HEAD 相同。诊断性 `tsc --noEmit --moduleResolution bundler --module esnext` 通过，不冒充原配置通过，也未改全局 tsconfig。
- `node scripts/check-i18n-usage.mjs`：仍是既有 NameTranslator OptionsPanel 的 DYNAMIC_KEY 和 STALE_MANIFEST，无 studio 错误；相关文件未修改。
- 未安装/升级依赖，未改变 pnpm-lock.yaml；未启动常驻 Vite 服务。Electron 测试均在 finally/afterAll 关闭实例。最终通过宿主只读 ps 检查本轮临时 userData、仓库 Electron/Vite/Vitest，匹配结果仅有查询命令自身，无残留测试进程，未关闭用户已有窗口。
- 最终 `check_spec.py --stage ready --require-approval --json` 为 0 errors / 0 warnings，scope digest 保持原授权的 26bde5f69e3431a2ff3329f8b0cbdf006aba57e7e2f29ced6ef37d87d0206013；当前 increment digest 为 94f918c0e042dc70fe2604f6ea0cd8f619bf8cf05e6e34163ab2cd0ad4c27390。`git diff --check` 通过。

## 后续接续

T03 的通用模型客户端已初步阅读：现有 `model-runtime-client.ts` 分派 Chat Completions/Responses，两个 adapter 默认额外重试 2 次；新版应显式关闭这一层重试，再由自己的批次任务拥有重试预算。实际请求 serializer 目前是 adapter 私有函数，实施时需提供受审计的共用序列化入口，确保预算和发出正文一致，不能仅估待译字符。

模型配置来自应用通用 `useModelStore`，需要在接入时审计其音频迁移传递依赖和凭据传递路径；本轮没有读取用户 API key，也未派发真实 API 请求。T03 尚未领取，后续先确认任务依赖版本和实际写集，再实现翻译协议、预算和验证。

## 风险与未执行项

T02 完成不代表 I1 整体验收；T03–T06 仍待实施。Windows 文件锁以故障注入验证，未在真实 Windows 运行，也未实测断电；记录中 sync/rename 不构成无条件抗断电承诺。模型翻译、取消恢复、多模式导出及 v1 共存/移除演练留在后续任务。全局原配置 tsc 与 i18n usage 的既有失败须在 I1 最终集成验收中处理。
