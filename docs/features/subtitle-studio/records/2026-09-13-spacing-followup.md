# 间距、分隔线与工具卡片

本轮人工反馈：

1. 左栏刷新、筛选和设置按钮尺寸一致，右侧及相邻间隔视觉均衡。
2. 预览文件标题行上下留白由 10px 调整至 6px，包含同排视图控件。
3. 翻译任务统计的纵横分隔线连接区域边界；检查结果和导出/翻译详情区的同类断线。
4. 工具列表卡片重写描述和标签，覆盖本地转写、AI 翻译及多格式导出。

实现与技术验证完成，未提交推送。沿用现有工作流程，仅调整本轮涉及的排版和四语言文案。

## 实现与验证

- 刷新/筛选/设置均为 28×28px；同帧实测标题上/右留白各 6px、搜索到筛选/筛选到设置/设置到右边缘均 6px。预览文件标题与同排 Tab 上下留白均 6px。
- 任务统计由单元格拥有内边距，纵线贯穿整行，token/取消状态/最近一轮任务使用连接容器两端的横线。保留正文留白。
- 统一操作结果的详情标题和条目分隔线、导出确认详情、翻译计划详情的通栏边界；详情仍默认折叠，既有键盘操作和结果流程保留。
- 卡片描述为“本地转写音视频、AI 翻译字幕，批量导出双语文件”；标签为本地转写、AI 翻译、SRT / LRC / VTT / ASS，同步四语言。
- 基于现有 `result-feedback-ui.test.ts` 运行本地额外几何/截图探针，最终 36 秒通过，覆盖实际导出、原文件保存、导入成功/失败、翻译提交、取消/删除、详情展开及中英/深浅/窄窗口状态。数据和翻译 API 均为隔离受控测试，实际使用生产 main/preload/仓库，不调用付费 API。
- 证据：`test-results/studio-i7-results/run-VFFrKp/`，含最终卡片、统计、导出确认和各结果截图以及同帧几何数据；日志 `test-results/studio-spacing-final-ui.log`。首轮工具卡片截图还处于 preload 加载层，未用作验收，最终截图已等待加载层退出后重取。
- TypeScript、四语言完整性和引用、preload、生产构建及 diff --check 通过。更新现有回归中与新详情布局和 token 总量展示相关的旧断言。
