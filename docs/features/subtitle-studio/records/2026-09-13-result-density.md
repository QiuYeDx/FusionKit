# 结果弹窗统一与紧凑化

用户反馈：结果提示的详情区应与“已选文档”一致，24px 及更大的外围留白应收紧为 12px；完成后提交推送。

实现：抽取共用文档折叠标题（文件图标、标题、数量徽标、箭头），结果详情与已选文档复用同一外框和交互样式。导出、原文件保存、导入、翻译提交、删除、恢复、取消的结果均通过 StudioOperationResult 生效。外层 header/content/footer 横向留白、header/footer 纵向留白、图标到标题和底栏按钮间距调整为 12px。详情默认折叠，保留失败优先、可读错误、键盘及关闭时的内容保留。

验证：

- 三端 Vite 构建、TypeScript、432 文件边界、preload 外部模块及 2,256 个 i18n 键检查通过。
- 实际 Electron 文档右键和即时导航两套回归通过。
- 结果流程在真实 main/preload/repository 上覆盖导出、原文件保存、部分导入、全部失败、取消请求、翻译提交、删除及批量导入；逐次确认默认折叠、关闭内容保留及 12px 几何尺寸。证据：`test-results/studio-i7-results/run-BP5q34/`，截图已检查。
- 完整结果套件多次在主题/语言热重载后的 preload 启动遮罩处超时，发生在不同重载点；未把中断记录为整套通过，也未修改本轮范围外的启动逻辑。相关日志为 `studio-result-density-ui*.log`。额外采用独立启动检查英文深浅主题，两种主题的默认折叠、展开清单、12px 留白均通过，截图已复核：`test-results/studio-result-isolated-7JNRKs/`。所有本轮测试进程和临时 profile 已清理。

本次提交同时包含此前已完成且待提交的右键菜单、即时切换、左栏密度与分隔线/工具卡片调整；历史记录继续保留对应快照。

## 居中反馈补充

按用户最新要求，共用结果弹窗的状态图标、标题和说明改为居中；隐藏右上角关闭图标，保留底部完成及原有主操作。只对结果态生效，配置表单的关闭入口不变；文件详情继续沿用已选文档的统一清单布局，外围 12px 留白保持。
验证：三端构建通过；独立实际 Electron 深浅主题检查通过（居中、叉号隐藏、默认折叠、展开清单、完成关闭及 12px 留白），截图已审阅，证据为 test-results/studio-result-centered-j7sfcm。
