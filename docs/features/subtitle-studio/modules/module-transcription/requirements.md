# I2 转写继承需求

## 目标与范围

承接 2026-09-11 用户在开发进度盘点后回复“好的，继续往后推进工作吧”。按已说明的顺序完成 I1 门禁收尾，进入 I2；当前展开生产基线冻结与基于该来源的独立机械复制/确定性回放，形成可以核对、重建、发现漂移的来源及副本。用户继续实施的指令不代替 I1 整体验收签署。

I2 的业务目标仍为 BR-03 和 transcription-fork.md：继承现有本地转写，最终直接产生新版字幕文档。当前清单是后续机械复制的输入，不代表新版已经可以转写。

## 需求

### R-TRANSCRIPTION-01 冻结有效生产来源与迁移边界

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

从实际已集成的 Git 提交冻结完整来源，覆盖主进程转写与生命周期、领域/IPC 类型、有效用户配置、原生资源 manifest/许可/构建脚本、相关测试及其 fixture。明确生产入口的传递依赖、应用组合层、需要替换的旧导出/交接边界和不采用的实验代码，记录拟议目的路径及理由。

#### 验收标准

- AC-TRANSCRIPTION-01-1: 对固定提交生成清单，两次结果逐字节相同；每项包含来源路径、Git 对象身份、内容 SHA-256、用途及拟议处置；缺文件、未解析的相对依赖和重复目的路径不能静默通过。
- AC-TRANSCRIPTION-01-2: 清单覆盖实际生产代码的相对/别名/type import 依赖和测试/脚本所需 fixture；应用组合层仅作为来源证据，不复制整个旧前端；动态加载、原生字符串和非代码资源边界有显式核对记录。
- AC-TRANSCRIPTION-01-3: 有效默认值、引擎/模型/VAD/CUDA 与平台 manifest 身份可追溯到冻结源码或资源；未存在的新副本 hash、原生推理结果、发布许可及平台通过结论不得伪造。
- AC-TRANSCRIPTION-01-4: 检查器可以发现已登记文件内容变化、遗漏和清单篡改；冻结历史提交后仍可重新校验；运行过程不执行旧业务模块、不读取真实 userData/模型/音频、不启动服务、不改源文件或依赖锁。

#### 边界与异常

Git blob 是跨 Windows/macOS 的字节身份；工作树状态核对遵循 Git 的换行规范，单独说明与历史提交验证的区别。大模型及未跟踪二进制仅记录可信 manifest 中的预期身份，不声称本机已验证实物。原生 C++、脚本和运行时字符串必须人工审计，AST import 图不能证明全部运行依赖。

### R-TRANSCRIPTION-02 独立机械副本与确定性等价回放

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-11 用户在T01完成、下一步明确为机械复制与等价回放后再次回复“好的，继续往后推进工作吧”。基于冻结的有效生产来源，建立新版完整主进程转写闭包、类型、默认配置、资源契约与必要测试；保留旧v1代码和全部上轮未提交结果。

#### 验收标准

- AC-TRANSCRIPTION-02-1: 从T01固定Git来源生成副本与实际provenance，每项记录源/目标内容hash及精确变换；只调整依赖路径、独立命名空间和必要测试定位，不调推理、句界、重试、增益、窗口或默认参数。重复检查可发现手改/缺失/目标冲突，默认不覆盖不同内容的目标。
- AC-TRANSCRIPTION-02-2: 新版生产源码及正常回归不import/type-import/调用旧业务或读取旧资源/测试helper；默认资源解析只指向新版根，IPC与权限实例独立。尚未复制的原生构建、真实资源及组合入口在provenance中明确递延，不用历史hash冒充新版运行或签名通过。
- AC-TRANSCRIPTION-02-3: 隔离迁移harness用同一固定响应、窗口、配置和增强响应运行冻结旧实现与新版副本，严格比较完整transcript、时间/词级证据、请求计划、窗口和回退/重试。覆盖fixed与acoustic、低音量原音回退/反例、合法重复与未知句界、跨窗重叠、CUDA f16 DTW资格及不合格路由；每类同时断言预期路径，不能只证明两边同样输出。
- AC-TRANSCRIPTION-02-4: 双方用真实构造/验证流程签发代表性的backend、accelerator、batch runtime及PCM/window凭据，本侧正例成功，对侧凭据被拒绝；不能用关闭brand验证的fake harness证明独立性。
- AC-TRANSCRIPTION-02-5: 新版确定性回归、实际依赖边界、TypeScript与既有I1回归通过；旧来源工作树对照无漂移。迁移对照可在独立维护测试中读取冻结Git到临时副本，但业务/正常测试不得依赖迁移工具或旧Git副本。副本尚不注册到产品入口，不声明用户已可转写。

#### 边界与异常

模型/VAD/CUDA上游内容身份保留，许可证及来源回执保留原字节与历史事实。只复制声明和预期身份，不生成伪造ready资源或使用v1安装目录。native addon命名空间与签名改动必须在后续重建后获取新hash，T02不加载旧实物作为新版证明。旧model-manager的move能力仅可暂留在未注册机械副本供继承验证，后续新版模型导入只允许copy，不能删掉用户选中的旧模型。

### R-TRANSCRIPTION-03 独立资源工厂、原生构建与打包组合

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

T02完成后用户再次要求“好的，继续往后推进工作吧”，承接独立资源、原生构建和进程接入的下一步。新增可独立调用的运行时工厂、新版原生构建/验证闭包和双工具打包入口；当前不注册renderer IPC、应用退出钩子或转写UI。文档生产者尚未就绪，不提前提供旧式文件转写界面。

#### 验收标准

- AC-TRANSCRIPTION-03-1: 原生源码及必要构建/校验脚本来自T01冻结Git，另记源/目标hash、准确变换和依赖；只读取新版源码/manifest。T01/T02来源和机械副本不改、不覆盖手工目标，原生事务前缀/组件/回执/签名标识对应新版契约。
- AC-TRANSCRIPTION-03-2: 运行时工厂仅用新版模块创建独立资源、权限、媒体和进程实例；managed根固定在调用方userData的subtitle-studio/transcription子树，无旧根扫描或PATH回退。构造不注册应用全局钩子/IPC或启动模型进程；初始化错误可观察。外部模型导入只能copy，拒绝move/未知字段，且不暴露可绕过facade的原manager。
- AC-TRANSCRIPTION-03-3: 同一工厂实例的初始化/关闭并发可收敛，终止先停止任务/资源操作再清理媒体/进程并释放session；失败可观察并支持必要重试。隔离测试覆盖两个实例/owner、错误/取消/关闭、缺资源和相邻模拟旧目录不被修改；模型导入经真实校验复制后可独立于源文件存在。
- AC-TRANSCRIPTION-03-4: 新增真实双资源配置与组合beforePack，检查完整实际配置的资源映射、支持平台与签名规则；逐贡献执行同等runtime/addon完整性验证，不向旧校验器传伪造缩减配置，也不改默认旧入口。缺失/篡改/错平台/错误映射必须阻止打包；移除旧贡献后新版验证无需旧脚本。
- AC-TRANSCRIPTION-03-5: 在当前macOS arm64主机真实编译新版addon、先最终嵌套ad-hoc签名再冻结hash，并在独立Electron进程加载/验证原生事务与恢复。Windows源码/契约静态覆盖但未实跑的平台明确待验；新普通回归、T02回放、真实依赖边界、两套TypeScript、i18n及旧来源无漂移通过。所有任务启动进程清理；不把ad-hoc测试当作发布签名、真实ASR或完整应用打包验收。

#### 边界与异常

本阶段只对隔离测试文件执行原生事务；不触碰真实用户文件、模型或安装。允许在本机测试目录生成新版原生实物和回执；跨平台实机、完整共存/删除后应用包和真实模型推理后续统一验收。T02精确副本保持可重建，外围新增组合层不要求删除历史证据或允许任意副本漂移。

### R-TRANSCRIPTION-04 最终转录结果直接生成文档

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-12用户在T03完成、下一步明确为转录结果接统一文档后再次要求“好的，继续往后推进工作吧”。实现主进程内部文档生产者及完整结果契约；转写任务准入/调度、资源实物、renderer转写入口留下一阶段。本轮只用隔离输入验证，不读取真实媒体或修改旧v1/T02副本。

#### 验收标准

- AC-TRANSCRIPTION-04-1: 新媒体文档采用明确版本和媒体来源、转录时间证据及结构化preservation；旧schema1字幕文档继续读写。完整canonical transcript与cue按原顺序一一对应，精确保留文本/起止和所有已存在word/probability/speaker/confidence/estimatedTiming、语言及model证据；不制造未知字段、媒体内容hash或SRT往返。
- AC-TRANSCRIPTION-04-2: 文档承接上限保持100000 cues及128MiB快照；100001条、字节/词数越界、非法时间或重复id明确拒绝并无文档发布；不截断、自动拆分、合并或重算时间。媒体transcript摘要明确为转录内容摘要，未复制媒体则无原媒体导出能力。
- AC-TRANSCRIPTION-04-3: 独立派生executor保留T02推理/窗口/后处理及brand逻辑，最终清理成功后返回完整transcript，文档生产者再释放batch pin并提交；输出目录/文件exporter不参与。来源变换可重建，T01/T02机械副本不改。回放证明最终transcript一致；失败/取消/清理失败不能调用sink或伪造artifact成功。
- AC-TRANSCRIPTION-04-4: main-only sink绑定逻辑operation及owner/generation、单次文档身份，提交前校验取消/有效性，同一操作并发收敛且内容漂移拒绝；失败可安全重试、已发布文档不因迟到取消伪报未提交，已删除文档不可重放复活。持久层发布后故障明确区分提交状态/持久性，不吞错误冒充完全持久化。
- AC-TRANSCRIPTION-04-5: 媒体文档可持久化重开、分页/翻译并投影为SRT/LRC，完整转录证据不随翻译改变；原字幕文件专用导出/双语拆分明确拒绝媒体，投影损失可观察。既有I1和T02回归、实际边界、两套TS/i18n、规格检查及进程清理通过。内部生产者验证不代表真实ASR或转写UI已经上线。

#### 边界与异常

当前不扩容已有文档和翻译检查点、不自动拆分长文档。发布前取消应无可读文档，提交边界之后取消只能阻止迟到事件交付，不回滚已提交结果；持久性未知须保留同一文档身份供重试核对。重启任务自动恢复、用户媒体授权和完整runtime任务调度后续接入，不声明跨重启网络/推理exactly-once。

### R-TRANSCRIPTION-05 受控任务准入、文档队列与应用接线

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-12 本分支接续盘点后，助手提出先完成任务准入、队列、生命周期及 main/preload 接线，用户回复“好的，继续推进工作吧”。本阶段将 T03 私有资源与 T04 文档生产者接成可调用的主进程流程，转写页面、真实 ASR/GPU 和发布验收随后展开。

#### 验收标准

- AC-TRANSCRIPTION-05-1: main 通过原生选择器授权媒体和 copy-only 模型导入；renderer 只能提交当前 owner 的不透明 token、模型配置和任务 ID，不能指定文件路径、owner、能力品牌或输出授权。固定 preload 方法沿用严格 capability、frame、URL 校验和旧 generic bridge 拒绝策略；撤销、重新注册、导航和窗口销毁使旧 owner 立即失效。
- AC-TRANSCRIPTION-05-2: 每次最多20份媒体按不可变配置整体准入，校验资源/运行时/后端身份、媒体身份和音轨；失败不留下半批队列或能力租约。并发提交保持准入顺序，当前运行时串行执行；待执行及运行中任务续租并持有资源忙状态，资源失效、取消或 owner 撤销不能绕过再次校验启动。
- AC-TRANSCRIPTION-05-3: 新队列逐任务调用真实 T04 producer/sink，结果为统一文档 ID，完成后可被现有文档库读取；不经过旧文件 exporter/artifact 或输出路径。完成回执优先于迟到取消；失败/取消不会伪造完成。摘要仅含有界显示信息、状态/进度和稳定错误码，不泄漏路径、token、模型内部身份或原始诊断。
- AC-TRANSCRIPTION-05-4: 支持查询、取消和终态移除；取消及 owner 释放同步阻止新工作并清理所有已签发能力。应用退出与更新等待新版/旧版全部运行时停止，失败保留可重试关闭与根锁，不提前报告清理完成。任务仅在本会话保留，不声明跨重启恢复；本阶段不提供推理重试按钮。
- AC-TRANSCRIPTION-05-5: 在隔离目录验证合法准入到真实文档仓库、FIFO、批量回滚、跨 owner、租约/资源失效、运行与排队取消、发布后取消及失败清理重试；复跑相关 I1/T02/T04、真实依赖边界、两套 TS、preload 构建和规格门禁。Windows 精确来源字节通过 LF 属性保持，不放宽摘要校验；隔离过程不使用真实用户数据，合成资源/推理不冒充真实 ASR。

#### 边界与异常

资源状态使用已存在的资源管理接口和静态校验语义；缺少新版原生 staging 时明确拒绝执行，应用仍可使用 I1。多媒体准入中的每个任务独立释放原生 batch pin，不承诺跨任务模型 pin 复用优化。用户媒体、模型和其他 owner 的可写资源不得通过错误恢复被删除。I3/I4、转写 UI、真实设备质量比较与安装包分发不属于本任务验收。

### R-TRANSCRIPTION-06 转写工作区与任务到文档交接

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-12 T05交付后，助手提出接入媒体选择、资源准备、转写参数和任务进度UI，用户再次回复“好的，继续推进工作吧”。本阶段实现现有工作台内的转写视图及隔离Electron交互验证。

#### 验收标准

- AC-TRANSCRIPTION-06-1: 工作台提供文档/转写切换，媒体原生多选最多20份，逐项显示名称、时长、音轨与探测失败；允许重新探测或移除，撤销失败保留有界重试。草稿、配置及任务观察在同一renderer会话的SPA切换后保留，不持久化token或宣称重启任务恢复。
- AC-TRANSCRIPTION-06-2: 使用新版资源API显示真实运行时、模型、VAD和可选加速资源状态，提供明确的copy-only模型导入、资源下载安装/取消和重新检查。缺资源或配置非法时说明原因并阻止提交；默认推理参数继承已冻结生产偏好，音轨与高级参数有界且不引入输出路径。
- AC-TRANSCRIPTION-06-3: 提交单飞且冻结捕获的媒体/配置，明确回执后消费草稿；传输结果未知时不得自动重复提交。任务轮询不能用迟到快照覆盖本地取消/移除/准入结果；展示实际阶段/进度及安全错误，支持取消和清理完成后的终态移除，不冒充运行成功。
- AC-TRANSCRIPTION-06-4: 已完成任务可发现并打开同一真实统一文档，兼容当前搜索/筛选/分页；发布持久性未知及清理待完成状态可见。现有文档预览、翻译、导出控制器不因视图或响应式分支失去活动状态。
- AC-TRANSCRIPTION-06-5: 复用共享布局、圆角Tab、面板、文件名及控件，桌面320px配置列、窄窗口媒体/队列优先；完成四语言、键盘/焦点、长名、错误、1280×860浅色和786×540深色实际Electron渲染检查。控制器反例、真实main/preload到文档仓库交接、已有模块回归、两套TS/i18n/构建/来源边界和规格检查通过；隔离合成runtime证据与真实ASR验收分开记录。

#### 边界与异常

不改变冻结推理副本、资源协议或旧版入口；新增重新探测方法仅接受当前owner的媒体token。资源缺失时不能通过renderer伪造就绪。本任务不下载实际大型模型、不做真实ASR/GPU质量结论、不发布或代签整体验收。

### R-TRANSCRIPTION-07 Windows 独立资源与真实 CPU 转写验证

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-12 T06交付后，下一步明确为完整FFmpeg/Whisper资源和有界真实对照，用户再次回复“好的，继续推进工作吧”。当前Windows主机有已固定的官方预编译输入、FFmpeg审计回执和模型，本阶段补齐独立资源制作工具并进行真实CPU短样本验证。

#### 验收标准

- AC-TRANSCRIPTION-07-1: 新增Windows CPU资源制作和addon构建/验证工具闭包，来自固定3a0f50e来源并单独记录精确变换、源/目标hash；不修改T01–T03冻结清单或副本。新版工具只引用新版源码/manifest/工具，输入显式指定；缺文件、错误hash、目标冲突拒绝，不自动发现或修改旧资源。
- AC-TRANSCRIPTION-07-2: 从固定版本上游输入及真实审计回执复制出独立FFmpeg、ffprobe、Whisper及12个DLL，完整许可/来源字节验证，生成新版manifest并通过实物启动探针。原始输入与旧staging不变；不复用旧ready结论，不以PATH替代资源，不把无模型探针当ASR。
- AC-TRANSCRIPTION-07-3: 从新版C++源码与匹配Electron工具链在Windows x64构建生产addon，冻结最终字节与构建回执，内容寻址staging后在真实Electron加载，并验证隔离事务、恢复及新版命名空间。生产实物不含测试故障入口，错误/清理可观察；新版完整资源通过实际贡献校验。
- AC-TRANSCRIPTION-07-4: 选用仓库已有可核对来源的短音频和固定模型，copy-only进入隔离新版资源根，真实CPU推理通过任务队列生成可重开的schema2文档。对同一短样本用固定旧生产链路和相同配置做有界对照，记录文字/时间/重复/遗漏差异和耗时；不调参掩盖回归，不将fixture或文本相等单独视为质量验收。
- AC-TRANSCRIPTION-07-5: 相关工具反例、来源重建、实际边界、两套TS和任务/文档回归通过；保留可复核实物hash与原始运行证据，关闭本轮进程。GPU、长音频/全质量矩阵、macOS资源及完整共存/移除安装包仍明确待验，不发布或代签整体验收。

#### 边界与异常

本阶段只读显式的仓库历史测试音频、模型和上游缓存；复制到新版build及隔离test-results目录，不读取真实userData，不改原音频/旧模型目录。真实推理依赖未就绪或样本无可核对来源时必须保留未验证状态。无需升级Whisper/FFmpeg或重启无限质量优化；CPU短样本证明链路可运行，不替代设备与完整质量验收。

### R-TRANSCRIPTION-08 默认 VAD 与 CPU/CUDA 有界真实对照

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I2 |
| 优先级 | P0 |

#### 说明

2026-09-12 T07交付并明确下一步为默认VAD及设备/质量矩阵后，用户再次回复“好的，继续推进工作吧”。本机六个既有样本、固定q5模型和CUDA ZIP已核对；缺少的885098字节VAD从固定提交公开URL取得并验证。本阶段执行固定矩阵，不重开历史质量调参。

#### 验收标准

- AC-TRANSCRIPTION-08-1: 在各自独立测试根，用真实新旧资源管理器copy-import模型、安装固定VAD和完整CUDA包，保留实际校验/解压/探针/原子提交/重新解析证据；仅下载传输可使用已核对本地缓存。固定输入hash和旧staging不变，不复用旧品牌证明、不改真实userData或冻结manifest。
- AC-TRANSCRIPTION-08-2: 固定A/B/C/B-noise/independent/full六样本，CPU与CUDA分别运行同模型large-v3-q5_0、ja、VAD=true、acoustic_quiet_v1及其余生产默认值的新旧配对。独立任务使用新鲜推理状态，真实CUDA必须有本次server PID的正GPU显存证明；保留请求/窗口及增益回退观察，不能用已安装状态或进程名代替设备证明。
- AC-TRANSCRIPTION-08-3: 新版每项通过实际runtime任务处理；成功结果经文档仓库完整canonical转录一一映射并在关闭后重开验证。无可识别内容须保留真实no_speech_detected失败且无文档，不伪造空转录或声学静音真值。保存原始新旧输出、配置、时间、设备和可定位差异；检查低声/噪声、停顿切点及无安全停顿回退的实际路径，不以cue数、SRT往返或文本相等单独证明识别质量。
- AC-TRANSCRIPTION-08-4: CUDA A每侧额外固定重复一次，审查任务隔离及非确定性影响；逐项审阅新旧差异与既有已接受局限，新增可证明回归不能自动接受。无差异只证明该固定样本的迁移保持。历史full两处重复及剩余分隔/可读性局限保留，不改VAD映射、阈值、特例或重跑额外crop搜索；空结果不直接断言无语音。
- AC-TRANSCRIPTION-08-5: 资源/对照工具的有意义反例、相关任务文档回归、冻结来源、实际边界、两套TS及规格检查通过；运行有界且结束清理本次所有进程，测试安装资源可安全移除，形成实物/源码/报告快照。完整应用共存/移除打包和跨平台发布验收仍待后续，不代签整体验收。

#### 边界与异常

只使用已核对的六份工作区样本；最长216秒，矩阵总素材499.993秒，每设备每侧相同。每项工作15分钟、清理45秒、资源安装10分钟有界，启动参数遵守生产120秒上限，整体测试90分钟上限。失败保留诊断、取消并join，不强杀别的任务进程；质量差异必要时只做一次固定A重复控制及证据分析，无法解释的新回归保持未完成。真实推理不注入模拟结果；没有UI变更，不把维护harness当完整应用端到端验收。

## 后续滚动展开

冻结与审查后，按“机械复制与确定性回放 → 新版资源/进程生命周期 → transcript 到文档 → 转写 UI → 有界真实对照 → 共存及移除打包演练”展开下一任务。保留已接受的转写质量局限，不重新启动无限调参；不引入 ASS、完整编辑器或共享可写模型目录。
