# I5 第二轮体验完善需求

来源为用户本轮十项问题及五图，见[原始记录](../../records/2026-09-12-experience-findings.md)。以已推送3855c2c为基线，不改写上一轮技术证据。

### R-EXPERIENCE-01 转写队列操作反馈

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

停止按钮在行hover上有独立视觉反馈，保留原有任务能力和禁用语义。

#### 验收标准

- AC-EXPERIENCE-01-1: 浅深主题行hover、按钮hover/active/focus-visible可区分，不靠标题提示替代反馈；禁用仍清晰。
- AC-EXPERIENCE-01-2: 运行、排队和终态动作沿用既有保护，鼠标与键盘可操作，窄窗不遮挡。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-02 转写媒体拖入

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

原生媒体拖入与按钮使用同一授权、探测和草稿流程。

#### 验收标准

- AC-EXPERIENCE-02-1: 多文件拖入有高亮和逐项结果；非文件、目录、无原生路径、超量、无音轨、部分探测失败及重复文件可处理；忙碌和退出不重复提交。
- AC-EXPERIENCE-02-2: preload同步捕获File，固定内部IPC不暴露任意路径，main逐次复核owner及能力；Windows临时代理仅在确定原始源后授权，转写来源目录仍指向真实音视频。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-03 可选自动翻译

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

默认关闭；启用后，对本次转写选择模型和目标语言并在提交时快照，成功文档由主进程衔接翻译。

#### 验收标准

- AC-EXPERIENCE-03-1: 默认不创建翻译任务；启用时模型/目标语言可见且校验完整，之后修改设置不改变已提交任务。失败或取消转写不发翻译请求。
- AC-EXPERIENCE-03-2: 每份已发布文档最多创建一次对应自动翻译任务；离开工具页仍继续，重复事件/查询不重复创建；翻译失败保留字幕和可见恢复入口。
- AC-EXPERIENCE-03-3: 待翻译意图和任务身份可追溯，缺配置或应用退出/重启导致中断时状态真实且可人工恢复；不得将密钥写入文档、任务摘要、偏好或日志。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-04 转写设置持久化

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

保存可复用转写设置和自动翻译偏好，应用重启可继续使用。

#### 验收标准

- AC-EXPERIENCE-04-1: 合法模型、设备、语言、任务模式、VAD、窗口策略和高级参数及自动翻译偏好在SPA/应用重启后恢复；首次自动翻译为关闭。
- AC-EXPERIENCE-04-2: 按版本校验及恢复默认处理损坏/旧数据，存储失败不阻止操作；不保存原生file token、排队运行状态或新增API key副本。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-05 运行环境状态布局

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

环境状态与检查操作使用紧凑、对齐的单行布局。

#### 验收标准

- AC-EXPERIENCE-05-1: 正常宽度内状态和重新检查同一行且垂直居中，长文案/窄窗必要时自然换行，不存在无意义大空隙。
- AC-EXPERIENCE-05-2: 检查中、未就绪和错误信息可理解，按钮焦点和禁用准确，不因资源短轮询反复跳动。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-06 导出默认配置

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

单份和批量导出默认双语、来源目录、未完成处使用原文。

#### 验收标准

- AC-EXPERIENCE-06-1: 进入普通导出时三项默认值一致；无译文或来源丢失时解释真实状态并允许补选/另选，不静默换成其他位置或失败行为。
- AC-EXPERIENCE-06-2: 用户可修改默认，原始下载语义保留；默认无额外内容后缀，同名仍编号防覆盖。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-07 导出文档与字段整理

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

文档与译文轨合并成末尾默认折叠列表，表单按内容组织宽度。

#### 验收标准

- AC-EXPERIENCE-07-1: 只显示一份文档清单，默认折叠、位于表单末尾；多轨才出现该文档选择，单轨无需选择或冗余说明；所选轨稳定参与最终导出。
- AC-EXPERIENCE-07-2: 滚动列表风格统一、有滚动感知边缘渐变；字段按相关性成组，不强制无意义全宽，深浅/窄屏/长文件名/键盘和折叠可用。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-08 译文工具条对齐

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

将译轨选择与清除、任务状态与操作分别分组。

#### 验收标准

- AC-EXPERIENCE-08-1: 宽屏选择与清除相邻，状态与控制对齐右端，空操作不占布局；状态长短变化不让清除按钮漂移。
- AC-EXPERIENCE-08-2: 窄屏按组换行，等待/失败/完成及无操作态保持对齐；清除、取消、恢复的既有保护不变。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-09 翻译进度静默更新

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

后台读取不触发全页忙碌，真实前台操作不被后台吞掉。

#### 验收标准

- AC-EXPERIENCE-09-1: 进度更新不翻转全页busy、不禁用无关操作、不清除已呈现正文/提示；保留当前文档、页码、滚动、焦点、译轨和弹窗，其他文档进度不重读同revision正文。
- AC-EXPERIENCE-09-2: 事件合并单飞且新变更最终追读，前台操作优先且点击不丢；旧查询/旧revision/删除/卸载不能回写新状态，真实错误可见并可恢复。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。

### R-EXPERIENCE-10 统一工作台文档列表

| 字段 | 值 |
| --- | --- |
| 来源 | BR-08 |
| 状态 | active |
| 批次 | I5 |
| 优先级 | P0 |

#### 说明

工作台弹窗和表单复用统一文档行及列表原语，保留各场景信息。

#### 验收标准

- AC-EXPERIENCE-10-1: 导出/翻译批量选择与摘要、任务总览及类似结果/确认清单使用共用视觉结构，文件名中间省略、元数据、对齐和间距一致；必要选择和操作保留。
- AC-EXPERIENCE-10-2: 滚动渐变不拦截点击、与主题匹配且边缘准确，短列表无多余遮罩；长短名称、空态、错误、disabled、focus及宽窄窗实际渲染通过。

#### 边界与异常

保留既有权限、文档修订与资源生命周期保护；验证使用隔离数据，用户整体验收独立记录。
