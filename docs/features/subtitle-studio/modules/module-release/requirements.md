# I9 本机实测与发布候选

2026-09-14 用户授权处理进度盘点中的1、2、4：补齐macOS资源、完整真实流程、恢复打包验证。I4继续后置，公开发布不在本轮。

### R-RELEASE-01 原生资源与实机工作流

| 字段 | 值 |
| --- | --- |
| 来源 | BR-03 |
| 状态 | active |
| 批次 | I9 |
| 优先级 | P0 |

#### 说明

补齐当前机器缺失的新版原生资源，并以明确来源语音验证完整生产链路。

#### 边界与异常

不更改ASR算法或固定来源pin，不修改用户原始媒体及模型；真实供应商不可用时保留待验证状态。

#### 验收标准

- AC-RELEASE-01-1: 从固定来源和可核对配方制作独立macOS arm64 runtime及addon，签名后生成hash；实际CPU/Metal和原生加载通过，原有资产保留，子进程全部退出。
- AC-RELEASE-01-2: 真实语音经生产转写链生成文档，关闭后重开，再翻译及单份/批量导出；保留源文件，准确区分实际API与受控HTTP证据。

### R-RELEASE-02 启动重载可靠性

| 字段 | 值 |
| --- | --- |
| 来源 | BR-01 |
| 状态 | active |
| 批次 | I9 |
| 优先级 | P0 |

#### 说明

针对已有真实Electron结果套件中的启动遮罩超时，恢复可靠的就绪与退出生命周期。

#### 边界与异常

保持现有动画视觉；未就绪renderer不提前揭幕，测试不得删断言或扩大超时冒充修复。

#### 验收标准

- AC-RELEASE-02-1: 找到主题/语言重载后遮罩超时原因，renderer真正就绪后可靠退出；隐藏后显示、延迟开始信号和连续重载均有真实Electron回归及截图，不能靠延长超时或删除断言掩盖问题。

### R-RELEASE-03 双资源发布候选

| 字段 | 值 |
| --- | --- |
| 来源 | BR-01 |
| 状态 | active |
| 批次 | I9 |
| 优先级 | P0 |

#### 说明

恢复双资源打包工作，制作和核对本地候选产物，明确正式发行所需证据。

#### 边界与异常

不公开发布。缺少Developer ID、公证凭据或Windows主机时记录真实缺项，不伪造通过；CUDA许可保持证据约束。

#### 验收标准

- AC-RELEASE-03-1: 默认build选择双资源配置并保留publish never；beforePack验证两套贡献；最终候选app包含正确runtime/addon，签名前后冻结hash一致，严格签名验证通过。
- AC-RELEASE-03-2: 整理FFmpeg来源材料、CUDA逐组件分发证据与正式签名/公证条件；有凭据和Windows目标机则执行对应真实验证，缺少则明确记录待验证及可执行步骤，不能称正式发行就绪。
- AC-RELEASE-03-3: 最终TS、i18n、边界、preload、适用来源审计、回归与规格检查通过；记录实际集成版本，当前文档与源码相符，清理全部本轮服务。
