# I6 交互与密度需求

来源见[本轮反馈](../../records/2026-09-13-interaction-findings.md)。按用户新要求替代I5导出清单位于末尾和原地显示检查结果的界面决策，保留后端保护及历史证据。

### R-INTERACTION-01 队列进度对比

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

浅深色hover时进度槽与行背景保持可见差异，0/部分/完成进度可辨。

#### 验收标准

- AC-INTERACTION-01-1: 浅深色hover时进度槽与行背景保持可见差异，0/部分/完成进度可辨。
- AC-INTERACTION-01-2: 保留停止按钮、键盘和取消能力，不以去掉行hover绕过问题。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-02 自动翻译提示密度

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

自动翻译为元数据行短标签，完整说明按需显示，不独占进度后的整行。

#### 验收标准

- AC-INTERACTION-02-1: 自动翻译为元数据行短标签，完整说明按需显示，不独占进度后的整行。
- AC-INTERACTION-02-2: 长名窄窗合理收缩换行，不遮挡状态/百分比/操作，错误仍可发现。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-03 明确复制语义

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

复制按钮打开菜单，明确选择原文、译文、双语及带时间信息，纯文本选项不暗含时间。

#### 验收标准

- AC-INTERACTION-03-1: 复制按钮打开菜单，明确选择原文、译文、双语及带时间信息，纯文本选项不暗含时间。
- AC-INTERACTION-03-2: 缺失/过期译文及未知结束时间按实际状态显示，不猜内容和结束时间；键盘、实际剪贴板和反馈正确。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-04 已选文档次序

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

导出已选文档位于编码与换行之前，默认折叠，多轨才显示选择。

#### 验收标准

- AC-INTERACTION-04-1: 导出已选文档位于编码与换行之前，默认折叠，多轨才显示选择。
- AC-INTERACTION-04-2: 共享编号与名称首行对齐，长名/操作/边缘渐变保留。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-05 导出明确确认步骤

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

检查后进入新标题的独立确认内容，只显示就绪/受阻数量、输出概况及合并变化摘要，文件详情默认折叠。

#### 验收标准

- AC-INTERACTION-05-1: 检查后进入新标题的独立确认内容，只显示就绪/受阻数量、输出概况及合并变化摘要，文件详情默认折叠。
- AC-INTERACTION-05-2: 明确确认按钮接受所示变化并实际导出，返回设置/取消/部分就绪/全部受阻/修订过期均可理解，保留真实plan和acceptedLosses，不在设置内偷偷追加勾选。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-06 简洁结果按需详情

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

导出/原始下载/批量翻译/导入/管理结果先显示简洁成功、失败或跳过总览，清单默认折叠。

#### 验收标准

- AC-INTERACTION-06-1: 导出/原始下载/批量翻译/导入/管理结果先显示简洁成功、失败或跳过总览，清单默认折叠。
- AC-INTERACTION-06-2: 失败详情和恢复入口保留；空结果/部分失败/长列表/键盘可用，不把实时任务监控当成结果隐藏。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-07 面板标题密度

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

同类面板header适度减少竖直留白，保留字号、操作热区与靠角协调。

#### 验收标准

- AC-INTERACTION-07-1: 同类面板header适度减少竖直留白，保留字号、操作热区与靠角协调。
- AC-INTERACTION-07-2: 只改工作台，宽窄浅深及长短状态可读，不全局压缩其他工具。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-08 视图切换稳定

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

文档/转写共享标题及顶部空间基准，来回切换标题和工作区顶部不大幅跳动。

#### 验收标准

- AC-INTERACTION-08-1: 文档/转写共享标题及顶部空间基准，来回切换标题和工作区顶部不大幅跳动。
- AC-INTERACTION-08-2: 处理真实滚动容器，不丢任务/选择，窄窗流程和底部导航可达。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-09 文档侧栏任务概览

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

翻译总览只显示在文档视图，宽窗置左侧独立紧凑面板，转写视图不显示。

#### 验收标准

- AC-INTERACTION-09-1: 翻译总览只显示在文档视图，宽窗置左侧独立紧凑面板，转写视图不显示。
- AC-INTERACTION-09-2: 活动/完成/失败及最近批次真实，查看全部可达；窄窗入口归属文档区域且不挤压正文。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。

### R-INTERACTION-10 全选局部更新

| 字段 | 值 |
| --- | --- |
| 来源 | BR-09 |
| 状态 | active |
| 批次 | I6 |
| 优先级 | P0 |

#### 说明

全选/清除不触发全页busy、读取状态、无关禁用或正文重读，当前已有页面选择立即响应。

#### 验收标准

- AC-INTERACTION-10-1: 全选/清除不触发全页busy、读取状态、无关禁用或正文重读，当前已有页面选择立即响应。
- AC-INTERACTION-10-2: 跨页/筛选/100上限保留，远程范围查询仅局部pending；重复操作/查询变化/删除/卸载不回写旧选择。

#### 边界与异常

保留既有来源、格式损失、任务和修订保护，使用隔离数据，不修改真实用户文件。
