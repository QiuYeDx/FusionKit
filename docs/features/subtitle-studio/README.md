# 字幕工作台：独立新版工具

日期：2026-09-08。工作名称：字幕工作台 / Subtitle Studio。工具身份 `subtitleStudio`，建议路由 `/tools/subtitle/studio`；这是新增入口，不改变现有入口的含义。

## 本轮结论

用户已认可统一字幕文档的方向，并授权实施独立新版 I1；现有工具作为 v1 保持可用，未来删除 v1 不影响新版。I1 七个工作包的技术实现与验证已完成：文档链路、持久化/权限、文本翻译、双语整理、恢复/取消和多模式导出，以及 Windows 真实旧版 CPU 转写、历史任务续跑、共存与实际移除构建的独立运行。2069 项相关回归通过；用户最终确认仍独立保留为 pending。本轮按要求停在 I1，不归档旧工具，不开始 I2。详见 [I1 收尾记录](records/2026-09-10-i1-closeout.md)。

2026-09-11 按用户补充需求完成 I1 的 `T-WORKSPACE-08`：异常提示可关闭并记忆，残留文档可查看位置和显式清除；文档库支持多文件导入、全库搜索/筛选/排序、分页与跨页选择；批量翻译、继续/取消、删除及单目录导出提供逐项结果，预览下载入口合并为统一图标菜单。333项模块测试和7个真实Electron场景通过，开发服务已清理；本轮实现基于 `b68477a`。详见 [文档库与批量体验记录](records/2026-09-11-library-batch.md)，仍停在 I1，用户整体验收 pending。

同日选择体验跟进已完成：主复选框跨页全选全部匹配文档，“选择本页”移入范围菜单；顶部与各行选择列实际对齐，100份上限就地说明。复跑开发版完整批量流程，并用最终构建版检查窄窗筛选全选与提示去重，详见上述记录末节；前轮全部测试数字不表示本次全部重跑。

三图视觉反馈跟进已完成：搜索与列表统一外边距，hover/当前预览/多选共用整行高亮；批量操作并入45px固定单行底栏，保留分页和数字跳页。真实开发版完整批量回归、构建版四语言100份选择的宽窄窗检查通过，测试进程已关闭。用户已认可本次界面优化并要求提交推送，详见上述记录最新一节；该反馈不替代整个I1的独立验收。

新版采用独立的领域模型、持久化、IPC、前端状态、执行队列、原生运行时与资源归属。字幕文件和本地转写都是文档的生产者；翻译写入译文轨；导出读取指定文档版本，不消费一次性的翻译结果字符串。

转写采用有来源记录的代码副本：先冻结现有生产实现及其依赖、默认值、原生资源和回归证据，复制后验证等价，再通过适配器输出新文档。不能只复制 Whisper HTTP 调用，也不允许新版运行时回调 v1。

## 阅读顺序与权威位置

| 文档 | 负责内容 |
| --- | --- |
| [业务范围](brd.md) | 用户约束、产品边界、分期目标 |
| [整体架构](architecture.md) | 独立边界、字幕模型、持久化、翻译与格式契约 |
| [转写继承与 v1 删除验证](transcription-fork.md) | 基线复制、效果保护、资源和打包归属、移除演练 |
| [第一批需求与验收](modules/module-workspace/requirements.md) | I1 的唯一需求与 AC |
| [第一批设计](modules/module-workspace/design.md) | I1 的实现落点、UI 与验证方法 |
| [第一批任务](modules/module-workspace/tasks.md) | 唯一任务状态、依赖、写集与验证计划 |
| [批次与授权](spec.json) | 当前增量、授权来源、待定决策 |
| [本轮设计记录](records/2026-09-08-design-review.md) | 代码基线、已核对事实、文档验证与未执行项 |

后续批次只定义目标与关键边界，不预建未使用模块或一次性展开所有实现细节。拟议 TypeScript/IPC 形状在实现后由对应类型与校验器成为字段级事实来源，文档保留设计理由与行为约束。

## 下一步

I1 的 `T-WORKSPACE-01` 至 `T-WORKSPACE-03` 已完成。T03 已接入独立文本翻译、实际请求预算、整批校验/提交、译文轨预览与用量，并完成授权的 DeepSeek 真实 API 验证，证据见 [T03 实施记录](records/2026-09-09-workspace-03.md)。

按 2026-09-09 用户双语文件反馈插入的 `T-WORKSPACE-07` 已完成：本地候选确认、原文方向/单行分界/复核筛选、导入译文轨、清轨后只用原文重新翻译；两份指定文件本地转换和 Electron 流程已验证，证据及语义歧义见 [T07 实施记录](records/2026-09-09-workspace-07.md)。T03/T07 及后续双语修复均已集成于 `9c88565`；历史验证记录保留当时源码快照，不代表 I1 整体验收。

2026-09-10 按用户继续推进的授权完成 `T-WORKSPACE-04` 和 `T-WORKSPACE-05`：失败/中断后按原计划恢复未提交批次，取消保护已保存译文，失效配置显式修复；下载原文件继续保留字节，另支持当前原文、纯译文和双语的 SRT/LRC 导出、编码/换行、损失确认与不完整策略。证据见 [恢复记录](records/2026-09-10-workspace-04.md) 和 [导出记录](records/2026-09-10-workspace-05.md)。本轮实现与验证随 `feat(subtitle-studio): add recovery and multi-mode export` 提交集成，历史记录保留验证时源码快照。

`T-WORKSPACE-06` 原有四语言 UI 与 macOS 移除验证见 [集成验证记录](records/2026-09-10-workspace-06.md)，既有 TypeScript/i18n/路径断言修复见 [检查修复记录](records/2026-09-10-check-failures.md)。随后用户授权仅完成 T06 和 I1 收尾：Windows 已补齐真实 CPU 转写、实际历史恢复、新版崩溃恢复和两种构建的 12 组合导出；同时修复 Windows 模型移动的 ctime 时序问题和必要测试/演练可移植性问题。T06 技术验证完成，当前记录见 [收尾记录](records/2026-09-10-i1-closeout.md)。后续用户明确要求提交并推送，本轮改动随 `fix(subtitle-studio): complete I1 Windows acceptance` 提交集成。用户最终确认仍 pending，不进入 I2、不发布。

T07 后续混合双语修复已验证：开启行内拆分时，结构双行中与另一侧完整重复的行内尾段也参与分离；用户 LRC 的 282 处重复尾段已修正。旧版已整理文档需重新导入原文件并再次确认，不自动改写已有结果。证据与保守识别边界见 [混合双语修复记录](records/2026-09-09-workspace-07-mixed.md)。

2026-09-10 又补齐双方均为完整混排的相同正文：用户文件中的 46 处现在可按字种分界拆分，预览与确认整理一致；13 处普通同文保持。整理弹窗支持输入页码后按 Enter 跳转，复用主表分页控件，按筛选结果计算页数。实际文件全部需复核预览页、Electron 截图位置及分页回归见 [预览与分页修复记录](records/2026-09-10-workspace-07-preview.md)。

2026-09-08 已收到用户开始 I1 的真实实施指令，授权来源登记于 spec.json，任务状态以 tasks.md 为准，证据见 [I1 实施记录](records/2026-09-08-i1-implementation.md)。按依赖推进就绪任务，不为内部命名或可逆技术细节重复询问。无需等待 ASS、完整编辑器或全量转写基准完成才开始 I1。

2026-09-11 用户实际 `pnpm dev` 验收发现历史文档读取失败会阻断整个工作台，以及 Vite 误扫 HTML 模板。已修复文档逐份错误隔离、初始化重试和应用入口扫描，补充真实开发版 Electron 验证；见 [开发启动修复记录](records/2026-09-11-development-startup-fix.md)。此前 T06 构建版证据不能代替本次开发环境历史数据验证。

## 换机器接续

本轮设计推送到 `v0.3.1` 分支。另一台机器先保护其未提交改动，拉取该分支的最新提交，在仓库根打开 Codex，按上方阅读顺序恢复上下文，不依赖本次聊天自动出现。

- 仓库文档、源码和已经跟踪的 `.agents/skills/fusionkit-pitfall-guard/` 随 Git 同步。
- 本机用户级 `spec-driven-ai-coding` 技能和它的 checker 不在本仓库，需要另一台机器已有或另行安装同版本。没有该辅助脚本时，可依本仓库明确的 R/AC/T/V、依赖、写集、授权及证据规则人工核验并记录“checker 未运行”；不要将工具缺失写成业务需求缺失，也不要冒充脚本通过。
- Codex 模型/推理设置、个人配置、连接器权限、API key 和应用偏好不由这次 Git 提交迁移。为减少行为差异，尽量使用相同模型和推理设置；不要把密钥提交到 Git。
- `node_modules`、构建输出、`test-results` 和本机受管原生资源不由 Git 同步。按 lockfile 安装依赖，按目标平台准备环境；I1 不要求先复制所有原生模型，I2 的真实转写对照需要另行准备模型和限定样本。
- 文档中的 Windows 绝对路径是历史环境记录，源码落点按仓库相对路径解析。新平台重新运行适用验证，不复用旧机器的通过结论。

可在新任务中发送：

```text
请在当前 FusionKit 仓库开始实现独立新版“字幕工作台”的 I1。
先阅读 docs/features/subtitle-studio/README.md，按其中索引读 spec.json、
BRD、整体架构、转写继承边界和 I1 的 requirements/design/tasks。
本条消息授权按已定义的 I1 范围开始实施；登记真实授权并更新任务台账。
核对任务台账和已集成版本后，从首个未完成任务开始，连续推进已就绪任务，执行验证并记录证据。
现有 v1 工具保持原状，新版不得依赖 v1；本轮不展开 I2 转写移植。
先核对代码基线、工作树和本机环境，保留他人改动；缺验证条件如实记录。
```

文档用于保持方向、约束和验收一致，不保证不同模型/环境生成逐行相同的实现。
