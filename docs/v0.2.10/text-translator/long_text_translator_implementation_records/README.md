# 长文本翻译工具实施记录

本目录保存 `long_text_translator_execution_plan.md` 中各工作包的逐会话实施记录。

规则：

1. 每次实现会话结束前新增或更新一份记录。
2. 文件名使用 `YYYY-MM-DD_<work-package-id>_<short-title>.md`。
3. 记录实际修改、接口变化、验证命令、结果、未完成事项和下一步。
4. 工作包只有在执行计划台账和实施记录一致时才能标记为 `已完成`。
5. 不在实施记录中写入 API Key、完整小说正文或完整模型返回。

实施记录模板以执行计划第 10 节为准。
